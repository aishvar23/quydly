# Learning record

Append-only journal. One section per story, written by
`tools/update_learning.py`. Newest at the bottom.

A `PROPOSAL (...)` indented sub-bullet flags an entry that needs
operator review at the next playbook revision — promotions are
deliberate, never auto-applied.

## Story 215 — 2026-05-06 — tech — outcome: skipped — q-
- **failure** — Stage 2 cleared a story that the synthesizer itself had already flagged as weak (quality_flags=['MIXED_STORY','NUMERIC_TRIVIA_RISK'], quiz_candidate=false, consistency_score=0.125, editorial_posture='disclosure_official'). The four publishability gates only read source counts, domain roots, verification_status, and casualty attribution — none of them inspect the synth's own quality fields. _(evidence: stories/215/02_evidence.json:status='ok'; stories/215/story.json:row.quality_flags, row.quiz_candidate, row.consistency_score, row.editorial_posture; video-learning/playbook/video-pipeline-playbook.md §1.3 (gates 1–4))_ → check: Add stage-2 gate-5 in video-learning/prompts/02-evidence-package.md 'Publishability decision': set status='insufficient' if `story.row.quiz_candidate === false` AND `story.row.editorial_posture === 'disclosure_official'`. Add gate-6: set status='insufficient' if `story.row.quality_flags` contains both 'MIXED_STORY' and 'NUMERIC_TRIVIA_RISK'. Add gate-7: set status='insufficient' if `story.row.consistency_score < 0.30`.
- **failure** — Stage 2 gate-2 ('all source_documents[].issuer collapse to a single domain root') passed strict letter but failed in spirit: 4/4 sources are 9to5 Network sister sites (9to5google.com ×3 + 9to5mac.com ×1) and 3/4 articles are by the same author (Justin Kahn). Effective source independence is single-perspective; reported source_diversity_score=0.64 ('diverse') overstated it. _(evidence: stories/215/story.json:row.source_documents (issuers, authors); stories/215/05_pre-render-critique.json:warns[1] check='sourcing/network-overlap'; video-learning/playbook/video-pipeline-playbook.md §1.3 gate-2)_ → check: Add stage-2 gate-2a in video-learning/prompts/02-evidence-package.md: set status='insufficient' if ≥75% of source_documents share a single `author` value. Add gate-2b: extend gate-2 from 'single domain root' to 'single editorial network' by maintaining a small list of known sister-site groupings (9to5google/9to5mac/9to5toys/electrek; vox.com/theverge.com; vice.com/motherboard.vice.com; conde-nast titles) in `tools/lib/source_networks.py`.
- **prompt_proposal** — Add an early-reject step 0 to the stage-1 type-inference walk so service-journalism / affiliate-deals aggregations never reach stages 2–5. _(evidence: video-learning/prompts/01-story-understanding.md `story_type` walk (steps 1–6); tools/lib/story_type.py:infer_story_type; stories/215/01_understanding.json:unknown_or_disputed[0])_ → check: Edit video-learning/prompts/01-story-understanding.md `### story_type` walk: insert as step 0 — 'If `editorial_posture === "disclosure_official"` AND `quality_flags` contains "NUMERIC_TRIVIA_RISK" AND `quiz_candidate === false` → write `_blockers.md` saying "service-journalism rejection" and stop. Do not produce 01_understanding.json.' Mirror the same guard in `tools/lib/story_type.py` by returning a new sentinel `"service-journalism"` from `infer_story_type` so `tools/process_story.py` exits non-zero (new exit code 5) before workspace creation.
  - PROPOSAL (prompt_proposal): review at next revision.
- **template_proposal** — Tech template's `fits_poorly_when` does not currently exclude retail-deals aggregation. The story_type inference defaulted 215 to 'tech' because category_id='tech' (rule 4 in tools/lib/story_type.py), but the tech template targets ship/incident/launch/outage/CVE — not retail discounting. _(evidence: video-learning/templates/tech.json:fits_poorly_when; tools/lib/story_type.py:lines 131–133 (the unconditional tech category match); stories/215/_meta.json:story_type='tech', stories/215/05_pre-render-critique.json:warns[0] check='tech/template-fit-soft')_ → check: Edit video-learning/templates/tech.json `fits_poorly_when` to add: 'the news is retail-discount aggregation or affiliate-marketing roundups (sources carry FTC affiliate-link disclaimers; primary signal is dollar-off-list, not a vendor decision)'. Edit tools/lib/story_type.py `infer_story_type`: before the tech category check at line 132, guard with `if posture == 'disclosure_official' and not row.get('factual_conflicts') and (row.get('quiz_candidate') is False): return 'service-journalism'`.
  - PROPOSAL (template_proposal): review at next revision.
- **prompt_proposal** — Tooling fix applied during story 215: schema and update_learning.py now treat 'skipped' as a first-class outcome (operator declined render after stage 5 cleared). Pending: matching update to 07-learning-extractor.md 'Outcome decision' table to document the new value. _(evidence: video-learning/schemas/learning-record.schema.json:properties.outcome.enum (now includes 'skipped'); tools/update_learning.py:_validate_workspace (now branches on outcome='skipped' to require pre_render but not post_render); stories/215/_meta.json:outcome='skipped'; stories/215/08_learning.json:outcome='skipped')_ → check: Edit video-learning/prompts/07-learning-extractor.md `## Outcome decision` table to add a row for 'skipped' with trigger 'operator declined render after stage 5 cleared (no stage-7 scrap, no 3-iteration cap)'. The schema and tool already accept the value as of 2026-05-05; the prompt is the remaining gap.
  - PROPOSAL (prompt_proposal): review at next revision.
- **prompt_proposal** — Stage 5's pre-render critic surfaced the network-overlap and template-fit-soft concerns as warns, not blockers — and warns do not stop the render. The check list (C1–C21) has no check that reads `story.row` quality_flags / quiz_candidate / consistency_score, so Stage 5 cannot promote synth-flag concerns to blocker status even when they are the load-bearing signal. _(evidence: video-learning/prompts/05-pre-render-critic.md check list (C1–C21); stories/215/05_pre-render-critique.json:warns[0..2]; stories/215/story.json:row.quality_flags)_ → check: Edit video-learning/prompts/05-pre-render-critic.md check list: add C22 'integrity/synth-flag-conflict' — if `story.json:row.quiz_candidate === false` OR (`row.quality_flags` contains both 'MIXED_STORY' and 'NUMERIC_TRIVIA_RISK') OR `row.consistency_score < 0.30`, append to blockers[] with the matching `check`. This is the stage-5 mirror of stage-2 gates 5–7; redundant by design so the gate fires even if stage 2 was patched on a stale prompt.
  - PROPOSAL (prompt_proposal): review at next revision.

## Story 224 — 2026-05-06 — geopolitics — outcome: rejected — q-
- **failure** — Stage-2 publishability gate-7 (consistency floor) fired on story 224: synth-reported consistency_score of 0.25 is below the 0.30 minimum, so the video pipeline rejected before stage 3. Reinforcing signals: quality_flags=['LOW_SUPPORT'], verification_status='draft', and the operation is referred to by two different code names across the two sources ('Project Freedom' on Trump's Truth Social post; 'Epic Fury' in Rubio's White House remarks). _(evidence: stories/224/02_evidence.json:reason; stories/224/story.json:row.consistency_score=0.25, row.quality_flags=['LOW_SUPPORT'], row.verification_status='draft'; stories/224/_blockers.md)_ → check: Gate-7 in video-learning/prompts/02-evidence-package.md ('Publishability decision' §7: insufficient if story.row.consistency_score < 0.30) — already in place since story 215; this is the second observed firing and the check works as intended. Operator may use this incident as the second data point when evaluating gate-7's hit rate against synth output volume.
- **prompt_proposal** — tools/lib/story_type.py inferred 'finance' for story 224, a clear geopolitics story (category_id='world'; primary_entities are Donald Trump, Strait of Hormuz, Iran, Marco Rubio — head of state and a foreign minister; no public company or central bank). The heuristic appears to weight a single non-empty structured_numbers.percentages entry (gasoline +50%) above the world-category + state-actor signal. The runner caught the mismatch via stage-1 type confirmation and the operator re-prepared with --story-type geopolitics --force, so no downstream artifacts were produced under the wrong template — but a future stage-2 rejection on a similarly mis-typed row would still create avoidable churn at stage 1. _(evidence: stories/224/_meta.json:story_type='finance' (initial, before --force re-prepare); stories/224/01_understanding.json:story_type='geopolitics' (correct, walked from prompts/01-story-understanding.md story_type rules: category_id='world' + state actors → geopolitics); video-learning/templates/geopolitics.json:fits_when (head of state / foreign ministry / alliance body / treaty mechanism))_ → check: In tools/lib/story_type.py, when category_id === 'world' AND primary_entities (or primary_entities_enriched[].type === 'person' with role mentioning president/minister/secretary, OR type === 'place' for a named state) is non-empty AND no entity is a public company or central bank, prefer 'geopolitics' over 'finance' — even when structured_numbers.percentages is non-empty. A consequence-percentage (e.g. retail gasoline) should not flip the type. Add a unit test that pins story 224 (Trump/Hormuz pause) to story_type='geopolitics' from the row alone.
  - PROPOSAL (prompt_proposal): review at next revision.

## Story 227 — 2026-05-06 — geopolitics — outcome: rejected — q-
- **failure** — Stage-2 publishability gate-7 (consistency floor) fired on story 227: synth-reported consistency_score=0.286 is below the 0.30 minimum, so the video pipeline rejected before stage 3. Reinforcing signals: quality_flags=['LOW_SUPPORT'], verification_status='draft', and an unreconciled name conflict for the paused U.S. escort operation in the Strait of Hormuz ('Project Freedom' on Trump's Truth Social post; 'Epic Fury' in Rubio's remarks). Third observed firing of gate-7 on a Trump / Hormuz / Iran shape (after stories 215 and 224). _(evidence: stories/227/02_evidence.json:reason; stories/227/story.json:row.consistency_score=0.286; stories/227/story.json:row.quality_flags=['LOW_SUPPORT']; stories/227/_blockers.md)_ → check: Gate-7 in video-learning/prompts/02-evidence-package.md ('Publishability decision' §7: insufficient if story.row.consistency_score < 0.30) — already in place since story 215; this is the second observed firing on a Trump/Hormuz/oil-percentage shape (after story 224). The check works as intended; no prompt change required. Operator may use this incident as a third data point when evaluating gate-7's hit rate against synth output volume.
- **failure** — Story 227 exhibits the same Trump / Hormuz / Iran / oil-percentage shape that produced story 224 (rejected at gate-7) and that was the discriminator-design target for the rule-5 finance-entity gate in tools/lib/story_type.py. The synth tagged story_type='finance_markets' (because the surface artifact is a 6% oil-price drop), but the underlying news is a state-actor / readout / de-escalation move; the finance-entity gate correctly rejected the auto-classification, surfacing the story as type='unknown' for operator override. Operator overrode to geopolitics in line with the story 224 decision. _(evidence: stories/227/_meta.json:story_type_provenance='explicit (--story-type)'; stories/227/story.json:row.story_type='finance_markets' vs row.primary_entities=['Donald Trump','Strait of Hormuz','Iran','Brent crude']; tools/lib/story_type.py rule-5 finance-entity gate (referenced in operator brief))_ → check: Continue to surface this row shape (head of state + named state counterparty + named theatre + oil-price magnitude) as story_type='unknown' rather than auto-classifying as finance. The current rule-5 boundary-aware finance entity matching (commit 551b348) is doing the right thing on this row; the operator's manual --story-type geopolitics is the correct escalation path. No prompt or template change required at this time; revisit if a third Trump/Hormuz/oil-percentage row arrives and gate-7 does NOT fire — that would be the signal the gate is not catching the consistency-driven cases the rule-5 gate hands off.
- **template_proposal** — Three Trump / Strait-of-Hormuz / Iran / oil-percentage rows in two days (215, 224, 227) have each failed publishability — twice on gate-7 (consistency floor) and once on gates 5/6 (synth-flag combinations). The synth keeps producing this shape and the video pipeline keeps rejecting it. Synth-side template work (not video-pipeline work) should investigate whether the upstream cluster + summarise prompts are structurally producing low-consistency rows for this exact shape (multi-source, conflicting operation names, mixed market-vs-policy framing) so the synth either suppresses the row or splits it into two stories before it reaches the video pipeline. _(evidence: stories/227/story.json:row.consistency_score=0.286 + row.quality_flags=['LOW_SUPPORT']; stories/224/* (gate-7 firing); stories/215/* (gate-5/6 firing per video-learning/learning/known-failure-modes.md))_ → check: Operator: when promoting the story 224 + 227 entries into LEARNING_RECORD.md, flag the Trump/Hormuz/oil-percentage shape as a synth-side template candidate. Concrete next step: between stories, audit the last 30 days of synth rows where primary_entities contains 'Strait of Hormuz' AND ('Donald Trump' OR a U.S.-cabinet-rank actor); for each, record consistency_score and stage-2 outcome. If ≥3 of those rows fail gate-7 within a 7-day window, file a synth-side template_proposal at the cluster prompt level (not the video prompts). This entry exists to make sure the pattern reaches the synth team rather than dying inside the video pipeline.
  - PROPOSAL (template_proposal): review at next revision.

## Story 181 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **failure** — All 7 modules rendered with asset_status='placeholder' because the new v2-bridge (scripts/render-from-plan.js) does not fetch external assets — hook and stakes placeholders triggered render/missing-asset blockers despite a clean stage-4 plan. _(evidence: stories/181/06_render-output/manifest.json:modules[0..6].asset_status (all 'placeholder'); stories/181/07_post-render-critique.json:blockers[0,1] (render/missing-asset on hook + stakes); stories/181/07_post-render-critique.json:infos[0] (bridge/v2-bridge-placeholder-universal))_ → check: Add a stage-7 pre-flight on manifest.json: if 100% of modules have asset_status='placeholder' AND manifest.bridge.version is set, log a single render/missing-asset blocker with check='bridge/asset-fetch-not-implemented' rather than one-per-module — and skip the per-module placeholder warns. This isolates the bridge gap from real plan-level asset misses. Promote scripts/render-from-plan.js to fetch Mapbox tiles for asset_hint='map' and resolve numeric_fact_ref data plates for asset_hint='data' before the next render run.
- **failure** — Close module overran by +18.572s (planned 8.0s, manifest 26.572s) because the OutroLockup component pads the tail of the video; ~20s of trailing silence after the spoken close ended. _(evidence: stories/181/06_render-output/manifest.json:modules[6].manifest_duration=26.572 vs planned_duration=8; stories/181/07_post-render-critique.json:warns[3] (render/mistimed-cut))_ → check: Add a stage-7 schema check: any module with |duration_drift_sec| > 5.0 is a blocker (current threshold ±1.0 produces a pacing finding, not a blocker). Separately, cap OutroLockup hold-frame duration in scripts/render-from-plan.js to 2.5s — let the close module's manifest_duration equal planned_duration + a fixed-cap brand hold rather than absorb arbitrary leftover voice/silence.
- **prompt_proposal** — Stage-7 prompt §'Asset / visual-safety check' rule for render/missing-asset is per-module (warn for evidence, blocker for hook/stakes). When the renderer is universally placeholder (a bridge limitation, not a plan defect), this produces 7 redundant findings that obscure the single root cause. _(evidence: video-learning/prompts/06-post-render-critic.md §'Asset / visual-safety check' line 'If manifest.json reports asset_status: "fallback" or "placeholder" for any module, that is a render/missing-asset finding — log to warns[] for an evidence module, blockers[] for a hook or stakes module.'; stories/181/07_post-render-critique.json:blockers[]+warns[] (4 missing-asset findings against 1 root cause))_ → check: Edit video-learning/prompts/06-post-render-critic.md §'Asset / visual-safety check': add a sub-rule that if manifest.bridge.version is set AND >=80% of modules have asset_status='placeholder', the critic logs ONE blocker keyed 'render/bridge-asset-gap' with detail naming the bridge version and the modules affected, and downgrades the per-module findings to a single info[] entry that lists the module indexes. This keeps the architectural gap visible without diluting plan-level findings.
  - PROPOSAL (prompt_proposal): review at next revision.
- **pattern** — Stage 4 plan held its text contract through render: every module's manifest text equals the planned text exactly, including the map module's place-name overlay 'India · Bangladesh · Dhaka' and the close's 'Dhaka's formal agrement is still pending'. _(evidence: stories/181/04_module-plan.json:modules[0..6].text vs stories/181/06_render-output/manifest.json:modules[0..6].text (verbatim match on all 7); stories/181/07_post-render-critique.json:module_findings[*].matches_plan=true)_ → check: Replicate: in stage 4, keep module.text fields as exact substrings of 03_script.md (or for kind=map, exact place-name list). The current C15 verbatim check in stage-5 (prompts/05-pre-render-critic.md) is doing the right thing — note as a confirmed-good pattern in known-good-patterns.md so future fresh Claude sessions stage-4 use it as the contract.
- **template_proposal** — Stage-5 already flagged the 'first since 1991' stakes claim as single-sourced (Times of India 84545 only), and post-render that fragility is now compounded — the data plate for the '1991' anchor never rendered (placeholder), so the claim survives only via voice. Geopolitics template should require explicit in-script attribution when a stakes-grade numeric_fact has source_ids.length === 1. _(evidence: stories/181/02_evidence.json:numeric_facts.last_political_neighbour_envoy_year (source_ids=['84545'], length 1); stories/181/05_pre-render-critique.json:warns[0] (sourcing/single-soft); stories/181/07_post-render-critique.json:warns[4] (post-render reinforcement); stories/181/03_script.md:Stakes paragraph (no 'according to' attribution))_ → check: Edit video-learning/templates/geopolitics.json: add stage_3_extra_check 'if any module with kind=stakes has a numeric_fact_ref whose 02_evidence.numeric_facts[ref].source_ids.length === 1, the script's Stakes paragraph must contain an explicit attribution string matching /according to (the )?[A-Z][a-z]+( [A-Z][a-z]+)*/ naming the issuer.' Stage 5 should fail-the-letter (currently passes by-letter, warns only) when this attribution is absent.
  - PROPOSAL (template_proposal): review at next revision.
- **failure** — All 6 source_documents are India-domiciled outlets — no Bangladeshi outlet, no Western wire, no multilateral readout — even though source_diversity_score=0.88 ('diverse') reads clean by domain count. Stage-5 flagged as warn; post-render does not change the gap. _(evidence: stories/181/story.json:row.source_documents[0..5].source_country (all 'in'); stories/181/05_pre-render-critique.json:warns[1] (evidence/source-pool-narrow); stories/181/07_post-render-critique.json:warns[5])_ → check: Add a stage-2 gate-2c in video-learning/prompts/02-evidence-package.md 'Publishability decision': for story_type='geopolitics' where 01_understanding.where lists ≥2 named countries, set status='insufficient' if 100% of source_documents share a single source_country. The story's editorial perspective collapses to one party even when domain diversity reads high. Similar in shape to gate-2b (single editorial network) but indexed on source_country rather than network membership.

## Story 183 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **failure** — Every module in story 183's render manifest carried asset_status='placeholder' because the v2-bridge (scripts/render-from-plan.js) does not fetch external assets — it sets placeholder unconditionally when module.asset.src is absent and never populates module.asset.src. This is a render-layer architectural gap, not a stage-3/4 plan defect, but it produces five render/missing-asset warns and caps subjective_quality at 3 regardless of story craft. _(evidence: stories/183/06_render-output/manifest.json:modules[*].asset_status (all 6 = 'placeholder'); scripts/render-from-plan.js:278 (assetStatus = m.asset && m.asset.src ? 'ok' : 'placeholder'); stories/183/07_post-render-critique.json:warns[0..3], infos[0])_ → check: Add a stage-7 pre-flight check in video-learning/prompts/06-post-render-critic.md: if 100% of manifest.modules[].asset_status === 'placeholder', log a single bridge/architectural-gap info entry and downgrade per-module render/missing-asset findings to warn (not blocker) regardless of module kind, but cap subjective_quality at 3 — the visual layer cannot be evaluated. Until the bridge fetches assets, do not promote any story to 'published' from a 100%-placeholder manifest.
- **failure** — The v2-bridge concentrated the entire 21.379s gap between the stage-4 plan total (53.0s) and the realised ElevenLabs TTS total (74.379s) into the close module (planned 7s, manifest 28.379s, drift +21.4s), leaving the outro on screen far longer than any single OutroLockup card can sensibly hold. _(evidence: stories/183/06_render-output/manifest.json:modules[5].manifest_duration=28.379 vs modules[5].planned_duration=28.379 (note: planned_duration in manifest is the bridge's *post-stretch* allocation, not the original 04_module-plan modules[5].duration_sec=7.0); stories/183/04_module-plan.json:modules[5].duration_sec=7.0; stories/183/07_post-render-critique.json:module_findings[5].duration_drift_sec=21.379, pacing_findings[0])_ → check: Add a stage-7 check: if any single module's (manifest_duration - 04_module-plan.duration_sec) > 5.0s while every other module's drift is <= 0.1s, log render/mistimed-cut and treat as iterate-blocker until the bridge re-balances. Bridge fix: scripts/render-from-plan.js should distribute the TTS-vs-plan delta proportionally across modules using TTS word-boundary timestamps, not absorb it into the final module.
- **prompt_proposal** — Stage-7 prompt (06-post-render-critic.md) treats any 'placeholder' asset_status on hook or stakes as a blocker, but does not distinguish bridge-layer architectural gaps (every module placeholder, indicating no asset-fetch step exists in the renderer) from stage-3/4 plan defects (a specific module's asset_hint failed). Operator had to reason about this distinction by hand for story 183; the prompt should encode it. _(evidence: video-learning/prompts/06-post-render-critic.md:81-83 ('If manifest.json reports asset_status: "fallback" or "placeholder" … blockers[] for a hook or stakes module.'); stories/183/07_post-render-critique.json:warns[0,1] (operator manually downgraded hook+stakes from blocker to warn citing the bridge gap))_ → check: Edit video-learning/prompts/06-post-render-critic.md § 'Asset / visual-safety check': add a precondition that before classifying placeholder severity, the critic must check if 100% of manifest.modules[].asset_status === 'placeholder'; if so, log one bridge/architectural-gap info, downgrade all per-module placeholder findings to warn, and cap subjective_quality at 3. The hook/stakes blocker rule applies only when the manifest contains a *mix* of 'ok' and 'placeholder' (i.e., the bridge can fetch assets but failed for the specific module).
  - PROPOSAL (prompt_proposal): review at next revision.
- **template_proposal** — Story 183 is sub-national domestic-election news (Kerala Assembly result) but was forced into the geopolitics template by tools/lib/story_type.py rule 5 (category_id='world' + named state actors). Geopolitics template hook patterns (treaty-tally, named-readout) are tuned for inter-state diplomacy; this story's hook is a move-actor-place pattern ('UDF swept Kerala's 140-seat assembly'), which fits the template loosely but not optimally. Stage-5 already surfaced this as 'template/fit' info. _(evidence: stories/183/05_pre-render-critique.json:infos[18] (template/fit warn, 'Story is sub-national domestic-election news; geopolitics template is the closest registered fit … no blocker; surface as template_proposal candidate'); stories/183/01_understanding.json:notes (same observation))_ → check: Add a new template at video-learning/templates/elections.json (or domestic-politics.json) covering: (a) hook pattern = 'alliance-swept-jurisdiction-on-date' or 'tally-broke-streak'; (b) stakes pattern = streak-break or majority-flip with year anchor; (c) close pattern = next leadership decision date/venue. Update tools/lib/story_type.py to add an 'elections' classification rule before falling through to rule-5 geopolitics: trigger when row carries (constituency / assembly / vote-count language) AND (winning-margin numeric_facts) AND (named alliance/party as primary entity).
  - PROPOSAL (template_proposal): review at next revision.
- **pattern** — Stage-5 caught a single-sourced votes margin (Vijayan's 19,247 in Dharmadam, source_ids=['102049']) at iteration 1 via check sourcing/single, and iteration-2 fix prepended 'According to Hindustan Times,' to Beat 1 of 03_script.md. The check fired correctly, the operator's targeted fix preserved the verbatim module text fragment 'Pinarayi Vijayan held Dharmadam by 19,247 votes' as a substring, and stage 5 cleared on iteration 2. The sourcing/single → explicit-attribution loop works as designed for vote-margin numbers (replicating the seed pattern that exists for casualty figures). _(evidence: stories/183/05_pre-render-critique.json:infos[0] (iteration record), infos[3] (sourcing/single resolution); stories/183/03_script.md:line 11 ('According to Hindustan Times, Pinarayi Vijayan held Dharmadam by 19,247 votes'); stories/183/04_module-plan.json:modules[2].text)_ → check: Replicate for other domestic-election stories: when a votes-margin numeric_fact has source_ids of length 1 AND the value is precise (not a rounded 'over X' phrasing), require explicit 'According to <issuer>,' or '<issuer> reports' attribution in the script sentence, not just <!-- src: --> trailing comment. Codify in video-learning/templates/elections.json once that template exists; in the meantime, add to video-learning/prompts/05-pre-render-critic.md C3 (sourcing/single) the language 'precise vote-margin or casualty figures with single source_id require explicit in-line attribution to the issuer name'.
- **failure** — Synth surfaced ZERO structured numbers for story 183 (story.row.structured_numbers={}, primary_entities_enriched=[]) despite the source articles carrying the 19,247-vote margin, 85,000-vote margin, 79.6% turnout, and the 1977 streak-break year. Stage-2 evidence package recovered 8 numeric_facts directly from source_documents content. Same upstream-extractor weakness pattern that the seed 'extractor count-drop' entry warns about; this is a second observed firing for that class on a non-finance story. _(evidence: stories/183/05_pre-render-critique.json:infos[20] (extractor/count-drop seed-failure-mode check, 'synth surfaced ZERO structured numbers … 02_evidence numeric_facts contains 8 entries pulled directly from source_documents content. Synth-side extractor weakness on this row'); stories/183/02_evidence.json:numeric_facts[*])_ → check: Stage-2 gate already exists ('A drop >=50% is a blocker for stage 2' per known-failure-modes.md seed entry). It did not fire here because the gate triggers on a drop, not on a zero-from-zero synth output where stage-2 successfully recovers via direct content scan. Refine the seed gate in video-learning/prompts/02-evidence-package.md: when story.row.structured_numbers is empty AND 02_evidence stage-2 recovery surfaces >=3 numeric_facts, log a synth/extractor-empty warn (not a blocker — stage 2 succeeded), and route the row to 08_learning as upstream synth feedback automatically. This converts the manual surfacing in 05's infos[20] into a structured signal.

## Story 187 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **failure** — Every module in the render reported asset_status='placeholder' because the v2-bridge has no external asset-fetch layer wired in, so a stage-4 plan with valid asset_hint values still produces an under-styled rendered piece. _(evidence: stories/187/06_render-output/manifest.json:modules[0..5].asset_status (all 'placeholder') and stories/187/07_post-render-critique.json:warns[0] (render/missing-asset across all 6 modules).)_ → check: Stage 7 prompt (video-learning/prompts/06-post-render-critic.md) should grow a 'bridge gap' clause: when manifest.bridge.version='v2-bridge' and asset_status='placeholder' on >=50% of modules, log a single render/bridge-asset-gap warn instead of one render/missing-asset warn per module, and explicitly suppress the 'blocker on hook/stakes missing-asset' rule so the story isn't iterated solely for the platform gap. Test: a regex over manifest.json for `"asset_status": "placeholder"` count >= modules.length/2 plus bridge.version='v2-bridge'.
- **prompt_proposal** — v2-bridge absorbs the entire voice-vs-plan duration overrun into the close module (story 187: +10.8s on a planned 8.0s close, beats 1-4 unchanged); stage 4 has no awareness of this redistribution behaviour, so plan duration_sec values for the close are systematically optimistic. _(evidence: stories/187/04_module-plan.json:modules[5].duration_sec=8.0 vs stories/187/06_render-output/manifest.json:modules[5].manifest_duration=18.813 (voice.total_duration_sec=64.413 vs plan total_duration_sec=54.0). Modules 0-4 each have duration_drift_sec=0.0 exactly.)_ → check: Add a 'close duration headroom' clause to video-learning/prompts/04-module-plan.md (Module duration rules section): for v2-bridge renders, the close module's duration_sec must equal max(planned_close, voice_estimate_sec - sum(planned_durations[0..n-1])); if the voice estimate is unavailable at stage 4, allocate >=15% of total_duration_sec to the close to absorb TTS variance. Test: stage 5 check C13 should also flag when modules[last].duration_sec / total_duration_sec < 0.15.
  - PROPOSAL (prompt_proposal): review at next revision.
- **pattern** — Hook 'Vijay's TVK won 107 seats' put a proper noun (Vijay/TVK) AND a digit-bearing token (107) in the first 4 words and was the strongest authored axis of the render — it survived stage-5 C8 cleanly and is the script's most replicable element on a geopolitics tally_official frame. _(evidence: stories/187/03_script.md:line 4 (hook), stories/187/05_pre-render-critique.json:infos[7] (C8 pass with first-4-words note), stories/187/04_module-plan.json:modules[0].text and stories/187/06_render-output/manifest.json:modules[0] (matches_plan=true, duration_drift=0).)_ → check: Replicate for any geopolitics story whose row carries editorial_posture='tally_official' and a single dominant numeric_fact: in stage 3, draft 2 candidate hooks and prefer the one whose first 4 words contain BOTH a proper noun AND a digit-bearing token (not just one). Add to video-learning/prompts/03-voiceover-script.md Hook rules — currently the rule asks for 'proper noun + concrete verb', tighten to 'proper noun + digit-bearing token' for tally_official-frame stories.
- **prompt_proposal** — Stage 1 mapped story.row.story_type='election_result' onto pipeline-registered 'geopolitics' via rule 5 (category_id='world' + named state actors); the geopolitics frame held through render but loses specificity for sub-national elections — the close cannot anchor a forward-window date because no template sub-type covers 'government formation talks'. _(evidence: stories/187/01_understanding.json:notes (rule 5 walk for geopolitics), stories/187/05_pre-render-critique.json:infos[22] (close-anchor 'this week' chosen because formation talks not yet scheduled), stories/187/05_pre-render-critique.json:infos[23] (type-classification-trace surfacing the gap).)_ → check: Add an 'election_result' story_type to video-learning/templates/ as a sibling of geopolitics: the close-template should accept either (a) a numeric majority-deficit anchor ('N seats short of M') OR (b) a named forward window ('formation talks open <date>'), and surface a stage-5 check that election_result close modules have at least one of those two anchor patterns. First occurrence — prompt_proposal not rule_proposal per two-strike rule.
  - PROPOSAL (prompt_proposal): review at next revision.

## Story 195 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **failure** — v2-bridge rendered every module of story 195 with asset_status='placeholder' despite stage 4 carrying numeric_fact_ref='defected_mps' and despite story.row containing wikipedia_thumbnail_url for Mann, Murmu, Chadha, and Punjab — the bridge does not perform any external asset fetch, so the visual layer is decoupled from the synth-side asset metadata that already exists. _(evidence: stories/195/06_render-output/manifest.json:modules[].asset_status (all 6 = 'placeholder'); story.json:row.primary_entities_enriched[].wikipedia_thumbnail_url (populated for 4/6 entities); story.json:row.structured_numbers.counts[0] (value=6, unit='MPs').)_ → check: Add a stage-7 check that, when manifest.bridge.version='v2-bridge' AND story.row.primary_entities_enriched[].wikipedia_thumbnail_url is populated for at least one named entity in any evidence module, the post-render critic must log a render/missing-asset warn citing the specific entity whose available thumbnail was not surfaced. Threshold: ≥1 named-entity beat with an available wiki_thumbnail rendered as placeholder.
- **failure** — Stakes module (module_index=1) carried numeric_fact_ref='defected_mps' (value=6, the headline number of the video) but rendered as a placeholder NumberCard — the single most consequential visual asset in the video landed generic. _(evidence: stories/195/06_render-output/manifest.json:modules[1].asset_status='placeholder', component_type='NumberCard', numeric_fact_ref='defected_mps'; stories/195/04_module-plan.json:modules[1].asset_hint='data'.)_ → check: Add a stage-7 blocker (not warn) when component_type='NumberCard' AND asset_status='placeholder' AND a numeric_fact_ref is present. Rationale: a stakes/data module is the only kind for which the placeholder fallback strips load-bearing meaning from the frame. Lives in video-learning/prompts/06-post-render-critic.md § 'Asset / visual-safety check'.
- **failure** — Outro module (module_index=5) rendered 20.4s for an 8.0s plan budget — a +12.4s drift, single-handedly pushing total runtime from planned 55.0s to actual 67.4s. The other five modules hit duration exactly. _(evidence: stories/195/06_render-output/manifest.json:modules[5].planned_duration=20.413, manifest_duration=20.413 (the manifest already encodes the over-long budget — the drift is between 04_module-plan.json:modules[5].duration_sec=8 and the manifest's 20.413s).)_ → check: Add a stage-6 (renderer) or stage-7 check: per-module |manifest_duration - plan_duration| > 2.0s logs a render/mistimed-cut warn naming the module index. Specifically for kind='close', drift > +5.0s suggests the renderer is silence-padding to flush voice or holding the end-card; route to a render/outro-pad failure subclass and ask whether the outro is data-determined or a renderer constant.
- **prompt_proposal** — Stage 4 (module-skeletons / plan) classified the verbatim Mann quote 'they cannot do as they please' as kind='evidence' rather than kind='quote', so the renderer applied EvidenceShelf instead of a quote-attribution treatment — speaker (Mann) is not surfaced on-frame for what is the only verbatim quote in the video. _(evidence: stories/195/04_module-plan.json:modules[4] (kind='evidence', text='they cannot do as they please'); stories/195/02_evidence.json:quotes[0] (text='cannot do as they please', speaker='Bhagwant Mann'); stories/195/06_render-output/manifest.json:modules[4].component_type='EvidenceShelf'.)_ → check: Edit video-learning/prompts/04-module-plan.md (or the relevant module-skeleton): add a rule that any module whose `text` is a substring of any 02_evidence.quotes[].text MUST set kind='quote' (not 'evidence'), so the renderer's quote-card treatment with attribution plate kicks in. Concrete check at stage 5: for every module, if module.text is a verbatim substring of any quote in 02_evidence.quotes[], assert module.kind='quote'; else log module/asset-mismatch.
  - PROPOSAL (prompt_proposal): review at next revision.
- **pattern** — Hook 'Bhagwant Mann petitioned President Murmu in Delhi' followed the geopolitics template (move-actor-place pattern) and the seed pattern (proper noun in first 4 words, concrete verb): first 4 words contain two proper nouns from 01_understanding.who and the verb 'petitioned' is concrete-procedural. Stakes 'According to Mann's representation, 6 Rajya Sabha seats are at stake' carries digit '6' per the seed pattern. Both checks (C8, C9) cleared at stage 5 without iteration. _(evidence: stories/195/05_pre-render-critique.json:infos C8 (hook/weak-anchor cleared), C9 (stakes/abstract cleared); stories/195/03_script.md lines 4 (Hook) and 7 (Stakes).)_ → check: Continue replicating: in stage 3 for geopolitics, draft hooks where the first 4 words contain ≥2 proper nouns from 01_understanding.who plus a procedural verb (petitioned, met, summoned, signed, vetoed). Stage 5 already enforces stakes/digit-bearing-token via C9.
- **rule_proposal** — Synth-side row coherence: story.row.key_points described an entirely different political event (2026 West Bengal Assembly result) than row.summary / row.headline / row.source_documents (Punjab CM petitioning the President over six AAP defectors). The video pipeline followed summary + source_documents correctly, but the row-internal mismatch is a class of synth defect that no current stage-2 gate inspects. This is the second observed key_points-vs-summary mismatch (operator should treat this as the second incident under the synth-row-coherence theme together with the unreconciled six-vs-seven MP count from related_stories 128/134). _(evidence: stories/195/story.json:row.key_points[0..9] (West Bengal election narrative) vs row.summary (Mann-Murmu meeting) vs row.source_documents 110397/109998 (Punjab story); stories/195/01_understanding.json:unknown_or_disputed (synth has merged two unrelated political stories into one row); stories/195/05_pre-render-critique.json:infos row-coherence/observation; stories/195/05_pre-render-critique.json:infos numeric-coherence/observation (six-vs-seven MP count).)_ → check: Add a stage-2 gate-8 in video-learning/prompts/02-evidence-package.md 'Publishability decision': set status='warn' (not insufficient — the video can still be made if summary/source_documents agree) when the top entity tokens of row.key_points have <50% overlap with the top entity tokens of row.summary. Concretely: tokenize both fields, drop stopwords, keep proper-noun candidates (capitalized 4+ char tokens); if Jaccard < 0.5, log sourcing/synth-row-incoherence and force the script to anchor on row.summary + row.source_documents and explicitly ignore row.key_points. The two-strike rule is satisfied: the seven-vs-six discrepancy from related_stories is the first incident, the West-Bengal-vs-Punjab key_points/summary mismatch on this same row is the second.
  - PROPOSAL (rule_proposal): review at next revision.

## Story 197 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **prompt_proposal** — Stage 4 module plans can reference numeric_facts whose value is a non-numeric type (e.g. an ISO date string), which the v2-bridge's NumberCard cannot animate via useCountUp; first render attempt for story 197 crashed on numeric_fact 'tribunal_set_up_date' (value='2026-04-05', unit='date') before the bridge was patched to type-guard chooseComponent. _(evidence: stories/197/02_evidence.json:numeric_facts[6] (id='tribunal_set_up_date', value='2026-04-05', unit='date'); stories/197/04_module-plan.json:modules[3] (numeric_fact_ref='tribunal_set_up_date'); stories/197/06_render-output/manifest.json:modules[3].component_type='EvidenceShelf' (post-patch route); stories/197/07_post-render-critique.json:infos[1] (bridge/numeric-fact-type-guard).)_ → check: Add a check in video-learning/prompts/04-module-plan.md under 'Module rules' that any numeric_fact_ref pointing at a numeric_fact whose unit is one of {'date','iso-date','timestamp','year-month','quarter'} OR whose value is a non-number must NOT be planned with asset_hint='data' that the bridge would route to NumberCard; route to EvidenceShelf or ImpactCard instead. Stage 5 should add check C16a: for every module with numeric_fact_ref set, assert typeof 02_evidence.numeric_facts[ref].value === 'number' OR plan asset_hint != 'data'. Block on violation.
  - PROPOSAL (prompt_proposal): review at next revision.
- **failure** — Every module rendered with manifest.asset_status='placeholder' because the v2-bridge has no external asset fetch step; per the post-render critic's per-kind rule, this generates a blocker on the hook and stakes modules and warns on evidence/close, forcing decision='iterate' on every story rendered through v2-bridge regardless of script craft. _(evidence: stories/197/06_render-output/manifest.json:modules[0..6].asset_status='placeholder'; stories/197/07_post-render-critique.json:blockers[0..1] and warns[0]; video-learning/prompts/06-post-render-critic.md 'Asset / visual-safety check' section: 'If manifest.json reports asset_status: "fallback" or "placeholder" for any module, that is a render/missing-asset finding — log to warns[] for an evidence module, blockers[] for a hook or stakes module.')_ → check: In tools/render via the v2-bridge, asset_status='placeholder' should be normalised to 'placeholder_by_design' until an asset-fetch step is added. The post-render critic prompt (video-learning/prompts/06-post-render-critic.md) should add a guard: if 100% of modules have asset_status='placeholder' AND manifest.bridge.version === 'v2-bridge', record once as infos[] 'render/architectural-gap' rather than fanning out as N blockers/warns. Failure class: render/missing-asset.
- **prompt_proposal** — Stage 4's duration calculation assumes 165 wpm but ElevenLabs cadence on this story ran at ~133 wpm, producing a +14.964s overrun (planned 60.5s, voiced 75.464s). The bridge then concentrated the entire overrun in the outro module (planned 8.5s, manifest 23.864s, drift +15.364s) rather than re-flowing across modules. _(evidence: stories/197/04_module-plan.json:total_duration_sec=60.5; stories/197/06_render-output/manifest.json:voice.total_duration_sec=75.464 and modules[6].planned_duration=23.864 (vs plan 8.5); stories/197/07_post-render-critique.json:warns[1] and infos[5].)_ → check: Update video-learning/prompts/04-module-plan.md duration formula from 'word_count / 165 * 60' to 'word_count / 145 * 60' (or expose as a constant) to better match observed ElevenLabs cadence. Stage 5 C13 (module/duration-drift) should additionally verify that no single module's planned_duration is less than 25% of the total voice duration absorbed if the bridge pins voice-tail to the last module — i.e. assert max(plan.modules[].duration_sec) >= 0.25 * sum(plan.modules[].duration_sec) is false unless intentional, OR change the bridge to re-flow.
  - PROPOSAL (prompt_proposal): review at next revision.
- **pattern** — Hook + stakes + 4-evidence + close at 60.5s planned and 7 modules total stayed at the geopolitics template cap and produced module-text fidelity of 100% (every module's rendered text == plan text byte-for-byte) and per-module duration drift of 0.0s on modules 0-5. _(evidence: stories/197/06_render-output/manifest.json: modules[0..5].manifest_duration === planned_duration on every row; modules[i].text === 04_module-plan.json:modules[i].text for i in 0..5 (manual diff). stories/197/07_post-render-critique.json:module_findings[0..5].matches_plan=true and duration_drift_sec=0.0.)_ → check: Replicate: for geopolitics-shaped domestic-policy stories, stage 4 should plan exactly 7 modules (hook, stakes, 4x evidence, close) with hook<=3s, stakes 5-8s, evidence 8-12s each, close 8-10s; total target 55-65s. Add as a recommended pattern in video-learning/templates/geopolitics.json under preferred_pattern, citing story 197.
- **template_proposal** — Story 197 is structurally a domestic election-result + judicial-intervention story but was processed under the 'geopolitics' template because the v2 taxonomy has no 'policy' or 'election-result' type. Stage 1 surfaced this in unknown_or_disputed; stage 5 surfaced it as info 'geopolitics/template-fit'; the geopolitics map module had to be deliberately omitted via the template's 'skip only if single-country domestic' note. _(evidence: stories/197/01_understanding.json:notes ('Synth tagged story_type=election_result which is not in the v2 enum...'); stories/197/05_pre-render-critique.json:infos[20] (geopolitics/template-fit, severity=info); stories/197/_meta.json carries inferred type 'geopolitics'.)_ → check: Add a 'policy' template at video-learning/templates/policy.json with hook patterns suited to domestic policy/election-result stories (no map module by default; close anchored on a backlog/comparator rather than scheduled date). Add walk-the-checks rule to tools/lib/story_type.py mapping synth's 'election_result' and 'policy' to type='policy' instead of falling through to 'geopolitics'. Two-strike rule applied: this is the first observed instance, so prompt/template proposal rather than rule_proposal.
  - PROPOSAL (template_proposal): review at next revision.

## Story 199 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **failure** — v2-bridge rendered every module with asset_status='placeholder' on a story whose row.primary_entities_enriched carries wikipedia_thumbnail_url for at least 6 named entities (Chadha, Maliwal, Harbhajan Singh, AAP logo, BJP logo, Kejriwal) — second consecutive geopolitics story (after 195) where the synth-side asset metadata is decoupled from the v2 video output. The bridge has no external asset fetch step at all, regardless of asset_hint or numeric_fact_ref. _(evidence: stories/199/06_render-output/manifest.json:modules[].asset_status (all 7 = 'placeholder'); story.json:row.primary_entities_enriched (lines 215, 226, 238, 250, 262, 274, 286 carry wikipedia_thumbnail_url); stories/199/07_post-render-critique.json:infos[render/architectural-gap].)_ → check: Promote story 195's stage-8 future_check to a playbook §6 decision rule (two-strike threshold met by story 199): 'When manifest.bridge.version='v2-bridge' AND ≥1 module has asset_status='placeholder' AND ≥1 named entity in 04_module-plan modules[].text has a populated row.primary_entities_enriched[].wikipedia_thumbnail_url, log render/missing-asset/bridge-gap as a warn (not blocker — architectural). When ≥2 stories in a row hit this, escalate the bridge-asset-fetch work item.' Threshold: 2 stories — already hit (195, 199).
- **failure** — Second occurrence of stakes-NumberCard placeholder: module_index=1 (component_type='NumberCard', numeric_fact_ref='mps_remaining' value=3) rendered as placeholder on the load-bearing '10 → 3' beat. Story 195 was the first occurrence (numeric_fact_ref='defected_mps', value=6); story 195's stage-8 entries[1] proposed promoting this exact pattern from warn to stage-7 blocker. Story 199 logged it as a blocker per that proposal — the future_check is empirically validated. _(evidence: stories/199/06_render-output/manifest.json:modules[1] (asset_status='placeholder', component_type='NumberCard', numeric_fact_ref='mps_remaining'); stories/199/07_post-render-critique.json:blockers[1]; stories/195/08_learning.json:entries[1] (the proposal this incident validates).)_ → check: Promote story 195's entries[1] proposal from prompt_proposal to a playbook §2 failure-class rule: add a row to the failure taxonomy table — class 'render/missing-asset/numbercard-placeholder', manifests as 'component_type=NumberCard with numeric_fact_ref AND asset_status=placeholder', check 'Stage 7 logs as blocker; bridge must populate the numeric value as a NumberCard data graphic before clearing.' Two-strike rule satisfied (195 + 199); ready to graduate from prompt to playbook.
- **failure** — Outro-pad drift hit +21.888s on a 10s-budget close — third consecutive incident (195: +12.4s, 197: +15.4s, 199: +21.9s) and now growing. The bridge pins the entire voice-track tail to the close module rather than re-flowing across modules. On story 199 the close occupies 31.888s / 83.388s = 38% of the runtime. _(evidence: stories/199/04_module-plan.json:modules[6].duration_sec=10; stories/199/06_render-output/manifest.json:modules[6].manifest_duration=31.888; stories/199/07_post-render-critique.json:warns[module/duration-drift] and pacing_findings[0]; cross-references stories/195/08_learning.json:entries[2] and stories/197/07_post-render-critique.json:warns (module 6 +15.364s).)_ → check: Stage 4 must lower the WPM assumption for duration calc from 165 to 120 wpm. Concretely in video-learning/prompts/04-module-plan.md § Duration: change `target_total = word_count / 165 * 60` to `target_total = word_count / 120 * 60`. Story 199 effective WPM = 118.6; story 197 = 132.8; story 195 ran similar. The 165 figure (playbook §1.1 Pace) is a *spoken-word target* for script length, not the *measured ElevenLabs cadence*. Stage 4 plan-budget should use the measured cadence so the close module's plan budget tracks the actual TTS dwell. Two-strike rule satisfied (197 noted the gap as an info; 199 confirms with a 28% drift).
- **pattern** — Stage 4 routed the verbatim Chadha quote 'I gave 15 years of my youth' to kind='quote' (component_type='QuoteCard'), confirming that story 195's stage-8 entries[3] prompt_proposal — 'any module whose text is a substring of any 02_evidence.quotes[].text MUST set kind=quote' — has been applied and works. The bridge picked QuoteCard, not EvidenceShelf. Rule 1 in the failure-mode-prevention chain (quote-routing) is now load-bearing. _(evidence: stories/199/04_module-plan.json:modules[4] (kind='quote', text='I gave 15 years of my youth'); stories/199/02_evidence.json:quotes[0].text contains the substring; stories/199/06_render-output/manifest.json:modules[4].component_type='QuoteCard'; stories/199/07_post-render-critique.json:infos[module/quote-routing].)_ → check: Continue to enforce: in stage 4, scan every drafted module's text against 02_evidence.quotes[].text; if substring, force kind='quote'. Stage 5 already has C4 (sourcing/paraphrase) which checks the inverse (kind='quote' → text must be in quotes[]); the forward check (text in quotes[] → kind must be 'quote') should be promoted alongside C4 in video-learning/prompts/05-pre-render-critic.md as C4a. Replicates across geopolitics where verbatim politician quotes are common.
- **pattern** — Hook 'Raghav Chadha led seven AAP MPs' followed both the geopolitics template (move-actor-place) and the seed pattern (proper noun + concrete verb in first 4 words): 'Raghav Chadha' (proper noun, 2 words), 'led' (concrete verb), 'seven' (digit-bearing number-word). Stakes 'AAP's Rajya Sabha bloc dropped from 10 to 3' carries three digit tokens (10, 3, 2/3) per the seed pattern. C8 cleared on the proper-noun branch with only a warn (digit-form preference) that did not manifest post-render. _(evidence: stories/199/03_script.md lines 4 (Hook), 7 (Stakes); stories/199/05_pre-render-critique.json:warns[hook/weak-anchor] (cleared on proper-noun branch), infos[stakes/abstract] (digit-bearing tokens 10, 3, 2/3).)_ → check: Continue replicating: in stage 3 for geopolitics, draft hooks where the first 4 words contain ≥1 proper noun from 01_understanding.who plus a concrete past-tense verb (led, signed, vetoed, summoned, petitioned, met). When a magnitude is the load-bearing fact, prefer the digit form ('seven AAP MPs' could have been '7 AAP MPs') — story 195's pattern proposed digit-leading hooks; story 199 used a number-word and still cleared, but the stage-5 warn surfaced the preference. Threshold: stage 5 raises hook/weak-anchor warn (not blocker) when first 4 words contain a number-word (one..ten, dozen, score) instead of a digit.
- **rule_proposal** — Stage-7 placeholder-blocker semantics need a documented carve-out: the prompt's per-kind rule mechanically marks hook+stakes placeholders as blockers, but for v2-bridge stories that gap is architectural, not a per-story craft defect — yet 'any blockers → decision=iterate' is the rule. Stories 195/197/199 all hit decision='iterate' because of the same architectural blocker, with no actual stage-3/4 work to redo. The system loses signal: 'iterate' should mean 'go back to stages 3-4 and fix the script/plan'; instead it means 'wait for the bridge'. Two-strike rule satisfied: this is the third occurrence (195, 197, 199) of bridge-driven iterate decisions where stages 3-4 are clean. _(evidence: stories/199/07_post-render-critique.json:blockers[0,1] (both render/missing-asset, both note 'architectural caveat: v2-bridge does not perform external asset fetch'); stories/197/07_post-render-critique.json:blockers (same 2 blockers, same caveat); stories/195/07_post-render-critique.json:warns (logged as warn, not blocker — operator chose differently because 195 was the first incident); video-learning/prompts/06-post-render-critic.md § decision rules ('Any blockers → decision: iterate regardless of subjective score').)_ → check: Add a fourth decision class to video-learning/prompts/06-post-render-critic.md § decision rules: 'awaiting-bridge' — fires when ALL blockers are render/missing-asset class AND infos[] contains a render/architectural-gap entry naming v2-bridge AND subjective_quality >= 3. Outcome label in 08_learning.json: extend the schema's outcome enum from {published, iterated, scrapped, rejected, skipped} to include 'awaiting-bridge', so stage 8 can distinguish bridge-blocked stories from real iteration loops. Two-strike rule met (195+197+199 = three occurrences). Without this carve-out, every geopolitics story until the bridge ships will close as 'iterated' with no actionable iteration work.
  - PROPOSAL (rule_proposal): review at next revision.

## Story 206 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **failure** — v2-bridge produced asset_status='placeholder' for all six modules, including the hook and stakes — bridge has no asset-fetch / asset-resolution code path, so every render is decoupled from the plan's asset_hint regardless of how strong the upstream evidence is. _(evidence: stories/206/06_render-output/manifest.json:modules[0..5].asset_status (all 'placeholder'); stories/206/07_post-render-critique.json:blockers[0..1] (render/missing-asset on hook and stakes); stories/206/07_post-render-critique.json:infos[0] 'architecture/v2-bridge-asset-gap'.)_ → check: Stage 7 must keep flagging asset_status='placeholder' on hook/stakes as render/missing-asset blockers (already does, per video-learning/prompts/06-post-render-critic.md 'Per-module check'). Until v2-bridge implements asset selection, every story routed through it will iterate at stage 7 — operator should treat this as a system-level signal to prioritise the bridge asset path rather than as a per-story script defect. Add a check at render-time entry: if manifest.bridge.version='v2-bridge' and any module.asset_class != 'mute', emit a startup warning that the bridge will placeholder all assets.
- **failure** — OutroLockup module absorbed +17.754s of trailing silent hold (planned 10s, manifest 27.754s) because the bridge sizes the outro to fill total runtime rather than ending after the close VO finishes. Net effect: ~16s of frozen outro after the narration ends at 72.354s. _(evidence: stories/206/06_render-output/manifest.json:modules[5] (planned_duration=10, manifest_duration=27.754); stories/206/06_render-output/subtitle-cues.json[25] (final cue ends 72.354s); stories/206/07_post-render-critique.json:warns[5] check='render/mistimed-cut'.)_ → check: Add a stage-7 module-finding rule (already partially implemented): if module.role='outro' AND |duration_drift_sec| > 5.0, log render/mistimed-cut as a blocker (currently warn). Add a render-time invariant in the bridge: outro_duration = max(close_vo_duration_sec + 1.0, 4.0) — never pad to fill an external total runtime target. Record check in video-learning/prompts/06-post-render-critic.md '§Pacing check'.
- **failure** — Module-boundary plan durations diverged from TTS reality after module 1: planned 11.5s/12s/10s/10s for evidence/evidence/quote/close, but ElevenLabs cadence pushed actual narrated boundaries 5–10s past plan boundaries (evidence-2 narration runs 28.7–45.5s vs plan 23–35s; quote runs 53.1–60.4s vs plan 45–55s). _(evidence: stories/206/04_module-plan.json:modules[2..5].duration_sec; stories/206/06_render-output/subtitle-cues.json (cues 11–25 narration timings); stories/206/07_post-render-critique.json:pacing_findings[2].)_ → check: Add a stage-4 sanity check in video-learning/prompts/04-module-plan.md: estimate per-module narration duration from word count at 2.7 words/sec (the empirical ElevenLabs cadence on this row: 151 words / 56.0s spoken-only = 2.7 wps), not the historical 2.5 wps. If sum(modules[].duration_sec) is within 2s of estimated TTS total, accept; otherwise re-budget. Threshold: |plan_total - 0.37 * word_count| <= 2.0 seconds.
- **prompt_proposal** — video-learning/prompts/06-post-render-critic.md does not tell stage 7 how to interpret a manifest where ALL modules are placeholder (the v2-bridge architectural-gap case). The current per-module rule produces 2 blockers + 4 warns mechanically, which is correct but the operator instruction had to override the decision rule by hand to avoid 'scrap' on a legitimate render. The prompt should explicitly handle this case so future fresh Claude sessions reach the same iterate-not-scrap conclusion without operator override. _(evidence: video-learning/prompts/06-post-render-critic.md '§Per-module check' (the unconditional rule that placeholder on hook/stakes is a blocker); stories/206/07_post-render-critique.json:infos[0] 'architecture/v2-bridge-asset-gap' and infos[4] 'decision/non-scrap'; manifest.bridge.version='v2-bridge'.)_ → check: Add a section to video-learning/prompts/06-post-render-critic.md (after §Per-module check): 'Bridge-version override: if manifest.bridge.version starts with 'v2-bridge' AND every module has asset_status=\'placeholder\', the placeholders are an architectural gap not a per-module render failure. Log a single architecture/v2-bridge-asset-gap blocker (not one per module), set decision=\'iterate\', and do NOT downgrade subjective_quality below 3 solely on the basis of placeholder assets — score the script/pacing/voice as if assets had landed, then subtract one point.' This makes the iterate-not-scrap decision automatic until the bridge gap is closed.
  - PROPOSAL (prompt_proposal): review at next revision.
- **pattern** — Hook landed at 0.0s per subtitle-cues.json[0].start — within the prompt's 0.5s tolerance. The first-4-words anchor 'The BJP took 207' (proper noun + verb + digit) per stage-5 C8 made the L1 read on time without a delay buffer. _(evidence: stories/206/06_render-output/subtitle-cues.json[0] (start=0.0, end=3.053, text='The BJP took 207 of 294'); stories/206/05_pre-render-critique.json:infos[7] 'C8/hook/weak-anchor' (first 4 words contain proper noun + digit); stories/206/03_script.md (Hook line).)_ → check: Replicate in stage 3 for geopolitics/finance: hook first-4-words must contain (a) a proper noun that is also in 01_understanding.who AND (b) a digit-bearing token. The C8 stage-5 check already enforces this; the post-render confirmation here is that when C8 passes the L1 lands on the 0.0s mark per ElevenLabs.

## Story 213 — 2026-05-06 — tech — outcome: iterated — q3
- **failure** — v2-bridge rendered every module of story 213 with asset_status='placeholder' (7/7 modules), turning the post-render check's hook/stakes asset rules into automatic blockers regardless of plan quality. _(evidence: stories/213/06_render-output/manifest.json:modules[0..6].asset_status (all 'placeholder'); stories/213/07_post-render-critique.json:blockers[0..1] (render/missing-asset on hook + stakes))_ → check: Add a manifest sanity check at the top of stage 7 in video-learning/prompts/06-post-render-critic.md: if manifest.bridge.version === 'v2-bridge' AND every module has asset_status='placeholder', surface this as a single 'render/v2-bridge-asset-gap' info finding plus per-kind warns/blockers per the existing rule. Operator should treat the architectural gap as a separate decision axis from per-story rejection — not scrap solely for this. Concretely: extend the prompt's 'Asset / visual-safety check' bullet for placeholders to read 'unless every module is placeholder AND manifest.bridge has no asset-fetch path declared, in which case downgrade hook/stakes blockers to warns and log one render/v2-bridge-asset-gap blocker against the bridge, not the story plan.'
- **failure** — Close/OutroLockup module overran by +25.4s (planned 8.7s, manifest 34.139s) because the bridge absorbed the entire voice-vs-plan timing residual into the outro slot rather than redistributing across body modules — total runtime 90.5s vs plan 65.1s. _(evidence: stories/213/06_render-output/manifest.json:modules[6].manifest_duration=34.139 vs planned_duration=8.7; voice.total_duration_sec=90.139 vs script-target 65.45s (180w / 165wpm))_ → check: Add a stage-4 check that compares the planned total module duration to an estimate of the actual TTS read time using a per-voice WPM constant (elevenlabs reads slower than the 165wpm playbook baseline). If estimated_voice_duration - planned_total > 5s, surface as a stage-5 warn so the script can be tightened before render. Concretely: in video-learning/prompts/04-module-plan.md add 'compute estimated_voice_duration = word_count / VOICE_WPM where VOICE_WPM defaults to 145 for elevenlabs' and require the plan's total_duration_sec to be within ±3s of that estimate, not the 165wpm script target. Threshold: |estimated_voice_duration - sum(modules.duration_sec)| <= 3.0.
- **prompt_proposal** — video-learning/prompts/06-post-render-critic.md does not currently distinguish between 'plan-side asset failure' (plan asked for a real asset and renderer failed to fetch it) and 'bridge-side asset gap' (bridge architecturally never fetches assets). Story 213 hits the second case but the prompt only describes the first, forcing every v2-bridge run to log hook+stakes blockers that have no story-level remediation. _(evidence: stories/213/07_post-render-critique.json:blockers[0..1]; video-learning/prompts/06-post-render-critic.md L74-83 ('Asset / visual-safety check' section))_ → check: Edit video-learning/prompts/06-post-render-critic.md 'Asset / visual-safety check' to add a sub-rule: 'If manifest.bridge.version indicates a bridge with no asset-fetch implementation (e.g. v2-bridge), and every module has asset_status="placeholder", log one render/v2-bridge-asset-gap blocker against the bridge and downgrade per-module hook/stakes placeholder findings to warns. Per-story decision should not be scrap solely on this basis.' First occurrence of this class — prompt_proposal not rule_proposal per the two-strike rule.
  - PROPOSAL (prompt_proposal): review at next revision.
- **prompt_proposal** — Stage-5 cleared story 213 to render even though the script's 180-word body at the playbook's 165wpm baseline implies a 65.45s read, while the elevenlabs voice actually delivers it in 90.1s — a 25s gap that the bridge dumped into the outro. Stage 5's C13 (duration-drift) only checks plan-vs-script, not script-vs-actual-voice. _(evidence: stories/213/05_pre-render-critique.json:infos[6] (C13/duration-drift, 'drift = -0.35s, within ±2.0s' against 165wpm baseline); stories/213/06_render-output/manifest.json:voice.total_duration_sec=90.139)_ → check: Edit video-learning/prompts/05-pre-render-critic.md C13 to compute drift against an elevenlabs-calibrated WPM (default 145) in addition to the 165wpm script target, and surface the larger of the two drifts. Threshold: warn if |sum(modules.duration_sec) - words/145*60| > 3.0s; blocker if > 6.0s. This would have surfaced the story 213 issue as a stage-5 warn before render, and stage 4 could have rebudgeted modules accordingly.
  - PROPOSAL (prompt_proposal): review at next revision.

## Story 216 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **failure** — Every module rendered with asset_status='placeholder' because the v2-bridge has no asset-fetch / NumberCard-binding step, so 'data' modules with valid numeric_fact_refs (M0 tvk_seats_won, M1 tvk_short_of_majority, M3 dmk_seats_won) ship without on-frame numerics and 'mute' modules report graphic-placeholder instead of an honored mute frame. _(evidence: stories/216/06_render-output/manifest.json:modules[0..5].asset_status (all 'placeholder'); stories/216/07_post-render-critique.json:warns[0..1] and infos[0..1])_ → check: Stage 7 must scan manifest.json for asset_status counts; if 100% of modules are 'placeholder' (architectural rather than per-module sourcing failure), classify as render/missing-asset-architectural and escalate the bridge-asset-fetch gap to the operator's stage-7 report. Add a one-line manifest summary 'asset_ok / asset_fallback / asset_placeholder counts' to manifest.json so the post-render critic can compute the architectural-vs-per-module distinction in O(1).
- **failure** — Outro module (M5, kind=close) ran 24.939s vs planned 8.0s — a +16.9s drift on a single module — driving total render duration to 72.939s and making the weakest beat (un-date-anchored close) the longest beat in the video. _(evidence: stories/216/06_render-output/manifest.json:modules[5].manifest_duration=24.939 vs stories/216/04_module-plan.json:modules[5].duration_sec=8.0; stories/216/07_post-render-critique.json:pacing_findings[0])_ → check: Stage 7: any module whose duration_drift_sec exceeds ±2.0 (currently the threshold is ±1.0 for pacing observation; tighten to ±2.0 for blocker on a single module) is a render/overflow blocker. Additionally: in stage 4, when total_duration_sec is computed, the close module's duration should be capped at min(planned_duration, total_voice_remaining_after_other_modules + 1.0s) to prevent the OutroLockup from absorbing all leftover composition time.
- **prompt_proposal** — Stage 5 C25 (geopolitics/close-anchor) flagged the un-date-anchored close as 'acceptable but weaker than a date-anchored close — surface for stage 7 review' but did not propose a length cap; the rendered outro then ran 3x its planned duration, compounding the weak anchor with a long tail. _(evidence: stories/216/05_pre-render-critique.json:infos[24] (geopolitics/close-anchor); stories/216/07_post-render-critique.json:infos[6])_ → check: Add a check to video-learning/prompts/05-pre-render-critic.md: when geopolitics/close-anchor lands in the 'acceptable-but-weaker, fallback applied' branch (no forward date in evidence), require the close module's planned duration_sec to be ≤6s (vs the default ≤8s) so the renderer cannot turn a weak close into the dominant tail. Encode as a stage-5 warn if the close is fallback-anchored AND module-plan.modules[close].duration_sec > 6.0.
  - PROPOSAL (prompt_proposal): review at next revision.
- **pattern** — Hook 'TVK won 108 of 234 Tamil Nadu' — first 4 words contain a proper noun (TVK) AND a digit-bearing token (108) AND a denominator framing (of 234) that encodes the stakes inside the hook itself. The hook L1 is 7 words, exactly at the per-kind cap, and reads as a fact, not a teaser. _(evidence: stories/216/03_script.md line 4 ('TVK won 108 of 234 Tamil Nadu seats Tuesday.'); stories/216/04_module-plan.json:modules[0].text; stage-5 C8 hook/anchor info)_ → check: Replicate for any electoral / vote-tally story: stage 3 should draft the hook as <party-acronym> + <verb> + <won-tally> + <of total-tally> + <place> + <time-tag>. Add this skeleton to video-learning/templates/module-skeletons/hook.md under a 'tally-hook' subsection so future fresh Claudes hit this shape on stories with denominator framing available in evidence.numeric_facts.
- **prompt_proposal** — Stage 5 had no check for the 'mute hint resolved as graphic-placeholder' bridge behavior; the bridge silently maps asset_hint='mute' to asset_class='graphic'/asset_status='placeholder' rather than to asset_status='ok' on a text-only frame, which means stage 5 cannot distinguish 'asset honored as mute' from 'asset failed and fell back'. _(evidence: stories/216/04_module-plan.json:modules[2].asset_hint='mute' and modules[4].asset_hint='mute' vs stories/216/06_render-output/manifest.json:modules[2].asset_class='graphic',asset_status='placeholder' and modules[4].asset_class='graphic',asset_status='placeholder')_ → check: Add to video-learning/prompts/06-post-render-critic.md per-module check: if plan.modules[i].asset_hint='mute' AND manifest.modules[i].asset_status='placeholder' AND manifest.modules[i].asset_class!='mute', log render/missing-asset info with detail 'mute-hint not honored as mute frame by bridge'. This is bridge-fix scaffolding; once the bridge maps mute→asset_status='ok',asset_class='mute', the check naturally becomes silent.
  - PROPOSAL (prompt_proposal): review at next revision.
- **pattern** — Stages 1–5 ran clean on a sub-national domestic-politics story (single state, single country) by explicitly invoking the geopolitics template's 'single-country domestic foreign-policy' branch to skip the map module — stage 5 C25 (geopolitics/map-module) recorded the rationale ('single-state theatre, no second geographic actor') rather than forcing a Tamil-Nadu map that would have added little. _(evidence: stories/216/05_pre-render-critique.json:infos[23] (geopolitics/map-module); stories/216/01_understanding.json:where=['Tamil Nadu','Chennai','New Delhi'] and who excludes any second-country actor; stories/216/04_module-plan.json has no kind='map' module)_ → check: Replicate for any geopolitics story whose 01_understanding.where is contained within a single country AND whose who[] contains no second-state actor: stage 4 may omit the map module; stage 5 must record the omission rationale in infos under check geopolitics/map-module. Add a one-line note to video-learning/templates/geopolitics.json under 'map module' guidance: 'For single-country sub-national stories, prefer data modules over a labelled-region map; record rationale in stage 5 infos.'

## Story 222 — 2026-05-06 — geopolitics — outcome: iterated — q3
- **rule_proposal** — Stage 4 module plans referenced numeric_fact 'ceasefire_deadline' (value='2026-04-22', unit='date') for two modules with asset_hint='data'; the post-197 bridge type-guard correctly routed them to ImpactCard (stakes) and OutroLockup (close) instead of NumberCard, so the render did not crash — but the underlying authoring gap (stage 4 freely points data-class modules at non-numeric numeric_facts) remains. This is the second observed firing on the same class after story 197. _(evidence: stories/222/02_evidence.json:numeric_facts[2] (id='ceasefire_deadline', value='2026-04-22', unit='date'); stories/222/04_module-plan.json:modules[1] and modules[5] (numeric_fact_ref='ceasefire_deadline', asset_hint='data'); stories/222/06_render-output/manifest.json:modules[1].component_type='ImpactCard', modules[5].component_type='OutroLockup'; stories/222/07_post-render-critique.json:infos[1] (bridge/date-valued-numeric-fact). Prior occurrence: stories/197/08_learning.json:entries[0] (prompt_proposal for date/iso-date/timestamp/year-month/quarter unit set).)_ → check: Two-strike threshold reached. Promote story 197's prompt_proposal into a hard rule in video-learning/playbook/video-pipeline-playbook.md §2 (failure taxonomy) under a new class 'plan/numeric-fact-type-mismatch'. Stage 4 (video-learning/prompts/04-module-plan.md): if any module's numeric_fact_ref points at a numeric_fact whose unit is in {'date','iso-date','timestamp','year-month','quarter'} OR whose value is not a JS number, the module's asset_hint must NOT be 'data' (route to 'mute' or document the asset as an annotated date-band/timeline). Stage 5 (video-learning/prompts/05-pre-render-critic.md): add check C16a — for every module with numeric_fact_ref set, assert typeof 02_evidence.numeric_facts[ref].value === 'number' OR module.asset_hint != 'data'; block on violation. The bridge type-guard remains as defense-in-depth.
  - PROPOSAL (rule_proposal): review at next revision.
- **failure** — Voice total 81.037s vs planned 60.4s — a +20.637s overrun on a 167-word script, the entire overrun pinned to the outro (module 5 plan=8.4s, manifest=29.437s, drift +21.037s). Effective ElevenLabs WPM = 123.6, the second consecutive geopolitics story to run well under the 165-wpm assumption used in stage 4's duration calculation (story 197 ran at 132.8 wpm). _(evidence: stories/222/06_render-output/manifest.json:voice.total_duration_sec=81.037; stories/222/04_module-plan.json:total_duration_sec=60.4; stories/222/07_post-render-critique.json:warns[0] (module/duration-drift) and pacing_findings[0..1]. Prior occurrence: stories/197/08_learning.json:entries[2] (prompt_proposal to lower the WPM constant from 165 to 145).)_ → check: Two-strike threshold reached on the wpm-drift class. Promote story 197's prompt_proposal: edit video-learning/prompts/04-module-plan.md duration formula from 'word_count / 165 * 60' to 'word_count / 140 * 60' (or expose as a constant `WPM_ASSUMPTION = 140` and reference it). Add a stage-5 check C13a: assert manifest-vs-plan voice overrun observed in last N geopolitics renders (rolling), and warn if predicted overrun > 10s. Failure class: render/wpm-drift (sub-class of module/duration-drift). Separately, the bridge's voice-tail-pin behavior should be addressed: re-flow the overrun across the last 2-3 modules rather than concentrating it on the outro alone.
- **failure** — All 6 modules rendered with manifest.asset_status='placeholder' because the v2-bridge has no external asset fetch step. Per the post-render critic's per-kind rule, this would normally generate blockers on the hook and stakes modules and warns on evidence/close; the special-case 'render/architectural-gap' guard recommended in story 197's 08_learning entry 2 was applied — placeholders recorded once as info rather than fanning out to N blockers — but the underlying gap remains: every v2-bridge render ships visually below the bar regardless of script craft. _(evidence: stories/222/06_render-output/manifest.json:modules[0..5].asset_status='placeholder' (6/6); stories/222/07_post-render-critique.json:infos[0] (render/architectural-gap), module_findings[0..5].asset_honored=false. Prior occurrence: stories/197/08_learning.json:entries[1] (failure on the same architectural-gap class).)_ → check: The post-197 prompt-side guard now works correctly (no spurious blocker fan-out). The remaining work is system-level: add an asset-fetch step to azure-functions/v2-bridge OR wire stage 4 asset_hints into a Remotion-side asset selector that pulls from a curated graphic library (labelled maps, generic data-viz templates, institutional seals). Until shipped, every v2-bridge story's subjective_quality is capped at ~3 because the visual evidence layer can't honor any asset_hint. Failure class: render/missing-asset (architectural).
- **pattern** — 6-module geopolitics plan (hook + stakes + 3xevidence + close, 60.4s target) renders with 100% module-text fidelity and zero per-module duration drift on modules 0-4. Same fidelity result as story 197's 7-module plan; argues that the v2-bridge's 'pin tail to outro' behavior is the only systematic source of plan-vs-render drift in the geopolitics template. _(evidence: stories/222/06_render-output/manifest.json: modules[0..4].manifest_duration === planned_duration on every row; modules[i].text === 04_module-plan.json:modules[i].text byte-for-byte for i in 0..5. stories/222/07_post-render-critique.json:module_findings[0..5].matches_plan=true; module_findings[0..4].duration_drift_sec=0.0.)_ → check: Replicate: for geopolitics-shaped breaking-readout stories with 1 named theatre and ≤3 numeric facts, stage 4 should plan 6 modules (hook 3-4s, stakes 7-9s, 3xevidence 11-15s each, close 8-10s) and rely on the bridge's per-module text/duration honoring. Add as a recommended pattern in video-learning/templates/geopolitics.json under preferred_pattern (story 197 used 7 modules with a 4-evidence run; story 222 used 6 with a 3-evidence run; both held text fidelity at 100%). Update the existing seed entry 'Three- or four-module evidence runs (vs. five+) rate higher' to cite both 197 and 222 as supporting evidence.
- **failure** — Story 222's source set is 6/6 India-based outlets (timesofindia.indiatimes.com x4, indiatoday.in x2) for a U.S.-Iran-Pakistan geopolitics story with no U.S., Iranian, or Pakistani primary state-readout sourcing. Stage 2 cleared the row (gate-3 source diversity passed because 2 distinct domain roots ≥ minimum) but stage 5 surfaced this as info context/source-geography. Survived to render unchanged, and although it did not produce a visible regression, it is a real source-quality weakness — the entire script's authority chain runs through wire-rewrites of U.S./Iranian official statements as filtered by Indian outlets. _(evidence: stories/222/story.json:row.source_documents (6 entries, all source_country='in', collapsing to 2 domain roots both Indian); stories/222/01_understanding.json:notes ('Source set is single-country (in) across two domains'); stories/222/05_pre-render-critique.json:infos[22] (context/source-geography, severity=info); stories/222/07_post-render-critique.json:infos[4] (stage5-warn-followup citing source-geography survival).)_ → check: Add a stage-2 publishability sub-gate in video-learning/prompts/02-evidence-package.md: for stories whose primary_geos do NOT include the source_documents[].source_country majority country, require ≥1 source from one of the named state-actor countries OR an authoritative international wire (Reuters, AFP, AP) for status='ok'; otherwise downgrade to status='ok-with-caveat' and require stage 4 to plan an explicit 'sourcing caveat' on-frame in the close, OR set status='insufficient' if the geopolitics row has zero state-actor sourcing AND zero international wire sourcing. Failure class: sourcing/geographic-mismatch (new sub-class of sourcing/single).
- **prompt_proposal** — Story 222's stage-2 publishability gates passed despite the synth's own quality_flags=['LOW_SUPPORT'], verification_status='draft', and consistency_score=0.333 (narrowly above the 0.30 gate-7 floor introduced after story 215). Stage 5 noted the narrow margin in context/related-story-shape (citing 224 and 227 as same-shape rejections at 0.25 and 0.286) but did not block. The cluster of 215/224/227/222 around a single Trump/Hormuz/Iran shape with three rejections at 0.30 and one narrow pass at 0.333 suggests gate-7's 0.30 floor may be set too low for this row shape. _(evidence: stories/222/story.json:row.consistency_score=0.333, row.quality_flags=['LOW_SUPPORT'], row.is_verified=false; stories/222/05_pre-render-critique.json:infos[20] (context/related-story-shape); video-learning/learning/known-failure-modes.md entries '[2026-05-06 story 224]' and '[2026-05-06 story 227]' both naming gate-7 firings on this shape at 0.25 and 0.286.)_ → check: Edit video-learning/prompts/02-evidence-package.md 'Publishability decision' §7: raise the consistency floor from 0.30 to 0.35, OR add a sub-rule that for rows where quality_flags includes 'LOW_SUPPORT' AND verification_status='draft', the consistency floor is 0.40. Track the next 3 firings — if the 0.40-conditional gate would have changed any of them to 'pass' or 'fail' wrongly, revisit. Two-strike rule: this is the first cluster-level operator concern about the gate's threshold (the prior 215/224/227 entries all reported gate-7 working as intended on hard-fail rows); a prompt_proposal here is the right escalation rather than a rule_proposal.
  - PROPOSAL (prompt_proposal): review at next revision.
