# Known good patterns

Each entry: `[<date> story <id>] <one-line pattern> — replicate: <how to>`.
Read by every fresh Claude before stage 3 and stage 4. Append-only.

When a pattern repeats across types, promote a template edit in
`video-learning/templates/<type>.json` or
`video-learning/templates/module-skeletons/<kind>.md`.

## Seed entries

- [seed] Hooks that lead with a proper noun + concrete verb in the first
  4 words land harder than time/place-first hooks. — replicate: in stage 3,
  draft 2 candidate hooks and prefer the one whose first 4 words contain
  a proper noun.
- [seed] Stakes lines that name the affected population *with a number*
  outperform abstract stakes. — replicate: in stage 3, the Stakes
  paragraph must contain at least one digit-bearing token.
- [seed] Three- or four-module evidence runs (vs. five+) rate higher in
  post-render review on 60–75s scripts. — replicate: in stage 4, prefer
  3 evidence modules unless the story has a quote *and* a map module, in
  which case 4 is fine.
- [2026-05-06 story 181] Stage 4 plan held its text contract through render: every module's manifest text equals the planned text exactly, including the map module's place-name overlay 'India · Bangladesh · Dhaka' and the close's 'Dhaka's formal agrement is still pending'. — replicate: Replicate: in stage 4, keep module.text fields as exact substrings of 03_script.md (or for kind=map, exact place-name list). The current C15 verbatim check in stage-5 (prompts/05-pre-render-critic.md) is doing the right thing — note as a confirmed-good pattern in known-good-patterns.md so future fresh Claude sessions stage-4 use it as the contract.
- [2026-05-06 story 183] Stage-5 caught a single-sourced votes margin (Vijayan's 19,247 in Dharmadam, source_ids=['102049']) at iteration 1 via check sourcing/single, and iteration-2 fix prepended 'According to Hindustan Times,' to Beat 1 of 03_script.md. The check fired correctly, the operator's targeted fix preserved the verbatim module text fragment 'Pinarayi Vijayan held Dharmadam by 19,247 votes' as a substring, and stage 5 cleared on iteration 2. The sourcing/single → explicit-attribution loop works as designed for vote-margin numbers (replicating the seed pattern that exists for casualty figures). — replicate: Replicate for other domestic-election stories: when a votes-margin numeric_fact has source_ids of length 1 AND the value is precise (not a rounded 'over X' phrasing), require explicit 'According to <issuer>,' or '<issuer> reports' attribution in the script sentence, not just <!-- src: --> trailing comment. Codify in video-learning/templates/elections.json once that template exists; in the meantime, add to video-learning/prompts/05-pre-render-critic.md C3 (sourcing/single) the language 'precise vote-margin or casualty figures with single source_id require explicit in-line attribution to the issuer name'.
- [2026-05-06 story 187] Hook 'Vijay's TVK won 107 seats' put a proper noun (Vijay/TVK) AND a digit-bearing token (107) in the first 4 words and was the strongest authored axis of the render — it survived stage-5 C8 cleanly and is the script's most replicable element on a geopolitics tally_official frame. — replicate: Replicate for any geopolitics story whose row carries editorial_posture='tally_official' and a single dominant numeric_fact: in stage 3, draft 2 candidate hooks and prefer the one whose first 4 words contain BOTH a proper noun AND a digit-bearing token (not just one). Add to video-learning/prompts/03-voiceover-script.md Hook rules — currently the rule asks for 'proper noun + concrete verb', tighten to 'proper noun + digit-bearing token' for tally_official-frame stories.
- [2026-05-06 story 195] Hook 'Bhagwant Mann petitioned President Murmu in Delhi' followed the geopolitics template (move-actor-place pattern) and the seed pattern (proper noun in first 4 words, concrete verb): first 4 words contain two proper nouns from 01_understanding.who and the verb 'petitioned' is concrete-procedural. Stakes 'According to Mann's representation, 6 Rajya Sabha seats are at stake' carries digit '6' per the seed pattern. Both checks (C8, C9) cleared at stage 5 without iteration. — replicate: Continue replicating: in stage 3 for geopolitics, draft hooks where the first 4 words contain ≥2 proper nouns from 01_understanding.who plus a procedural verb (petitioned, met, summoned, signed, vetoed). Stage 5 already enforces stakes/digit-bearing-token via C9.
- [2026-05-06 story 197] Hook + stakes + 4-evidence + close at 60.5s planned and 7 modules total stayed at the geopolitics template cap and produced module-text fidelity of 100% (every module's rendered text == plan text byte-for-byte) and per-module duration drift of 0.0s on modules 0-5. — replicate: Replicate: for geopolitics-shaped domestic-policy stories, stage 4 should plan exactly 7 modules (hook, stakes, 4x evidence, close) with hook<=3s, stakes 5-8s, evidence 8-12s each, close 8-10s; total target 55-65s. Add as a recommended pattern in video-learning/templates/geopolitics.json under preferred_pattern, citing story 197.
- [2026-05-06 story 199] Stage 4 routed the verbatim Chadha quote 'I gave 15 years of my youth' to kind='quote' (component_type='QuoteCard'), confirming that story 195's stage-8 entries[3] prompt_proposal — 'any module whose text is a substring of any 02_evidence.quotes[].text MUST set kind=quote' — has been applied and works. The bridge picked QuoteCard, not EvidenceShelf. Rule 1 in the failure-mode-prevention chain (quote-routing) is now load-bearing. — replicate: Continue to enforce: in stage 4, scan every drafted module's text against 02_evidence.quotes[].text; if substring, force kind='quote'. Stage 5 already has C4 (sourcing/paraphrase) which checks the inverse (kind='quote' → text must be in quotes[]); the forward check (text in quotes[] → kind must be 'quote') should be promoted alongside C4 in video-learning/prompts/05-pre-render-critic.md as C4a. Replicates across geopolitics where verbatim politician quotes are common.
- [2026-05-06 story 199] Hook 'Raghav Chadha led seven AAP MPs' followed both the geopolitics template (move-actor-place) and the seed pattern (proper noun + concrete verb in first 4 words): 'Raghav Chadha' (proper noun, 2 words), 'led' (concrete verb), 'seven' (digit-bearing number-word). Stakes 'AAP's Rajya Sabha bloc dropped from 10 to 3' carries three digit tokens (10, 3, 2/3) per the seed pattern. C8 cleared on the proper-noun branch with only a warn (digit-form preference) that did not manifest post-render. — replicate: Continue replicating: in stage 3 for geopolitics, draft hooks where the first 4 words contain ≥1 proper noun from 01_understanding.who plus a concrete past-tense verb (led, signed, vetoed, summoned, petitioned, met). When a magnitude is the load-bearing fact, prefer the digit form ('seven AAP MPs' could have been '7 AAP MPs') — story 195's pattern proposed digit-leading hooks; story 199 used a number-word and still cleared, but the stage-5 warn surfaced the preference. Threshold: stage 5 raises hook/weak-anchor warn (not blocker) when first 4 words contain a number-word (one..ten, dozen, score) instead of a digit.
- [2026-05-06 story 206] Hook landed at 0.0s per subtitle-cues.json[0].start — within the prompt's 0.5s tolerance. The first-4-words anchor 'The BJP took 207' (proper noun + verb + digit) per stage-5 C8 made the L1 read on time without a delay buffer. — replicate: Replicate in stage 3 for geopolitics/finance: hook first-4-words must contain (a) a proper noun that is also in 01_understanding.who AND (b) a digit-bearing token. The C8 stage-5 check already enforces this; the post-render confirmation here is that when C8 passes the L1 lands on the 0.0s mark per ElevenLabs.
- [2026-05-06 story 216] Hook 'TVK won 108 of 234 Tamil Nadu' — first 4 words contain a proper noun (TVK) AND a digit-bearing token (108) AND a denominator framing (of 234) that encodes the stakes inside the hook itself. The hook L1 is 7 words, exactly at the per-kind cap, and reads as a fact, not a teaser. — replicate: Replicate for any electoral / vote-tally story: stage 3 should draft the hook as <party-acronym> + <verb> + <won-tally> + <of total-tally> + <place> + <time-tag>. Add this skeleton to video-learning/templates/module-skeletons/hook.md under a 'tally-hook' subsection so future fresh Claudes hit this shape on stories with denominator framing available in evidence.numeric_facts.
- [2026-05-06 story 216] Stages 1–5 ran clean on a sub-national domestic-politics story (single state, single country) by explicitly invoking the geopolitics template's 'single-country domestic foreign-policy' branch to skip the map module — stage 5 C25 (geopolitics/map-module) recorded the rationale ('single-state theatre, no second geographic actor') rather than forcing a Tamil-Nadu map that would have added little. — replicate: Replicate for any geopolitics story whose 01_understanding.where is contained within a single country AND whose who[] contains no second-state actor: stage 4 may omit the map module; stage 5 must record the omission rationale in infos under check geopolitics/map-module. Add a one-line note to video-learning/templates/geopolitics.json under 'map module' guidance: 'For single-country sub-national stories, prefer data modules over a labelled-region map; record rationale in stage 5 infos.'
- [2026-05-06 story 222] 6-module geopolitics plan (hook + stakes + 3xevidence + close, 60.4s target) renders with 100% module-text fidelity and zero per-module duration drift on modules 0-4. Same fidelity result as story 197's 7-module plan; argues that the v2-bridge's 'pin tail to outro' behavior is the only systematic source of plan-vs-render drift in the geopolitics template. — replicate: Replicate: for geopolitics-shaped breaking-readout stories with 1 named theatre and ≤3 numeric facts, stage 4 should plan 6 modules (hook 3-4s, stakes 7-9s, 3xevidence 11-15s each, close 8-10s) and rely on the bridge's per-module text/duration honoring. Add as a recommended pattern in video-learning/templates/geopolitics.json under preferred_pattern (story 197 used 7 modules with a 4-evidence run; story 222 used 6 with a 3-evidence run; both held text fidelity at 100%). Update the existing seed entry 'Three- or four-module evidence runs (vs. five+) rate higher' to cite both 197 and 222 as supporting evidence.
