# Known failure modes

Each entry: `[<date> story <id>] <one-line failure> — check: <future test>`.
Read by every fresh Claude before stage 1 and stage 5. Append-only.

When a failure repeats, add a decision rule in
`video-learning/playbook/video-pipeline-playbook.md` referencing the
entries that caused it.

## Seed entries (carried over from prior pipeline work)

- [seed] Synthesizer-side weakness presents as empty extractor outputs in
  `02_evidence.json` (numeric_facts/quotes empty even though the story has
  numbers in `summary`/`key_points`). — check: stage 2 must compare the
  *count* of `structured_numbers`/`primary_entities_enriched` from
  `story.row` against the count surfaced in `02_evidence`. A drop ≥50% is
  a blocker for stage 2.
- [seed] Hook written as a question or hedged assertion. — check: stage 5
  must scan `03_script.md`'s Hook section for question marks and the banned
  word list (`could`, `may`, `seems`, `appears`, `what if`).
- [seed] Single-sourced casualty figure rendered without attribution. —
  check: stage 5 must verify any conflict-type story with a casualty
  number has either ≥2 source_ids or an explicit "according to <body>" in
  the script.
- [seed] Stakes line uses generic populations ("the world", "everyone"). —
  check: stage 5 scans `03_script.md`'s Stakes section for those literal
  phrases and rejects.
- [2026-05-06 story 215] Stage 2 cleared a story that the synthesizer itself had already flagged as weak (quality_flags=['MIXED_STORY','NUMERIC_TRIVIA_RISK'], quiz_candidate=false, consistency_score=0.125, editorial_posture='disclosure_official'). The four publishability gates only read source counts, domain roots, verification_status, and casualty attribution — none of them inspect the synth's own quality fields. — check: Add stage-2 gate-5 in video-learning/prompts/02-evidence-package.md 'Publishability decision': set status='insufficient' if `story.row.quiz_candidate === false` AND `story.row.editorial_posture === 'disclosure_official'`. Add gate-6: set status='insufficient' if `story.row.quality_flags` contains both 'MIXED_STORY' and 'NUMERIC_TRIVIA_RISK'. Add gate-7: set status='insufficient' if `story.row.consistency_score < 0.30`.
- [2026-05-06 story 215] Stage 2 gate-2 ('all source_documents[].issuer collapse to a single domain root') passed strict letter but failed in spirit: 4/4 sources are 9to5 Network sister sites (9to5google.com ×3 + 9to5mac.com ×1) and 3/4 articles are by the same author (Justin Kahn). Effective source independence is single-perspective; reported source_diversity_score=0.64 ('diverse') overstated it. — check: Add stage-2 gate-2a in video-learning/prompts/02-evidence-package.md: set status='insufficient' if ≥75% of source_documents share a single `author` value. Add gate-2b: extend gate-2 from 'single domain root' to 'single editorial network' by maintaining a small list of known sister-site groupings (9to5google/9to5mac/9to5toys/electrek; vox.com/theverge.com; vice.com/motherboard.vice.com; conde-nast titles) in `tools/lib/source_networks.py`.
- [2026-05-06 story 224] Stage-2 publishability gate-7 (consistency floor) fired on story 224: synth-reported consistency_score of 0.25 is below the 0.30 minimum, so the video pipeline rejected before stage 3. Reinforcing signals: quality_flags=['LOW_SUPPORT'], verification_status='draft', and the operation is referred to by two different code names across the two sources ('Project Freedom' on Trump's Truth Social post; 'Epic Fury' in Rubio's White House remarks). — check: Gate-7 in video-learning/prompts/02-evidence-package.md ('Publishability decision' §7: insufficient if story.row.consistency_score < 0.30) — already in place since story 215; this is the second observed firing and the check works as intended. Operator may use this incident as the second data point when evaluating gate-7's hit rate against synth output volume.
- [2026-05-06 story 227] Stage-2 publishability gate-7 (consistency floor) fired on story 227: synth-reported consistency_score=0.286 is below the 0.30 minimum, so the video pipeline rejected before stage 3. Reinforcing signals: quality_flags=['LOW_SUPPORT'], verification_status='draft', and an unreconciled name conflict for the paused U.S. escort operation in the Strait of Hormuz ('Project Freedom' on Trump's Truth Social post; 'Epic Fury' in Rubio's remarks). Third observed firing of gate-7 on a Trump / Hormuz / Iran shape (after stories 215 and 224). — check: Gate-7 in video-learning/prompts/02-evidence-package.md ('Publishability decision' §7: insufficient if story.row.consistency_score < 0.30) — already in place since story 215; this is the second observed firing on a Trump/Hormuz/oil-percentage shape (after story 224). The check works as intended; no prompt change required. Operator may use this incident as a third data point when evaluating gate-7's hit rate against synth output volume.
- [2026-05-06 story 227] Story 227 exhibits the same Trump / Hormuz / Iran / oil-percentage shape that produced story 224 (rejected at gate-7) and that was the discriminator-design target for the rule-5 finance-entity gate in tools/lib/story_type.py. The synth tagged story_type='finance_markets' (because the surface artifact is a 6% oil-price drop), but the underlying news is a state-actor / readout / de-escalation move; the finance-entity gate correctly rejected the auto-classification, surfacing the story as type='unknown' for operator override. Operator overrode to geopolitics in line with the story 224 decision. — check: Continue to surface this row shape (head of state + named state counterparty + named theatre + oil-price magnitude) as story_type='unknown' rather than auto-classifying as finance. The current rule-5 boundary-aware finance entity matching (commit 551b348) is doing the right thing on this row; the operator's manual --story-type geopolitics is the correct escalation path. No prompt or template change required at this time; revisit if a third Trump/Hormuz/oil-percentage row arrives and gate-7 does NOT fire — that would be the signal the gate is not catching the consistency-driven cases the rule-5 gate hands off.
- [2026-05-06 story 181] All 7 modules rendered with asset_status='placeholder' because the new v2-bridge (scripts/render-from-plan.js) does not fetch external assets — hook and stakes placeholders triggered render/missing-asset blockers despite a clean stage-4 plan. — check: Add a stage-7 pre-flight on manifest.json: if 100% of modules have asset_status='placeholder' AND manifest.bridge.version is set, log a single render/missing-asset blocker with check='bridge/asset-fetch-not-implemented' rather than one-per-module — and skip the per-module placeholder warns. This isolates the bridge gap from real plan-level asset misses. Promote scripts/render-from-plan.js to fetch Mapbox tiles for asset_hint='map' and resolve numeric_fact_ref data plates for asset_hint='data' before the next render run.
- [2026-05-06 story 181] Close module overran by +18.572s (planned 8.0s, manifest 26.572s) because the OutroLockup component pads the tail of the video; ~20s of trailing silence after the spoken close ended. — check: Add a stage-7 schema check: any module with |duration_drift_sec| > 5.0 is a blocker (current threshold ±1.0 produces a pacing finding, not a blocker). Separately, cap OutroLockup hold-frame duration in scripts/render-from-plan.js to 2.5s — let the close module's manifest_duration equal planned_duration + a fixed-cap brand hold rather than absorb arbitrary leftover voice/silence.
- [2026-05-06 story 181] All 6 source_documents are India-domiciled outlets — no Bangladeshi outlet, no Western wire, no multilateral readout — even though source_diversity_score=0.88 ('diverse') reads clean by domain count. Stage-5 flagged as warn; post-render does not change the gap. — check: Add a stage-2 gate-2c in video-learning/prompts/02-evidence-package.md 'Publishability decision': for story_type='geopolitics' where 01_understanding.where lists ≥2 named countries, set status='insufficient' if 100% of source_documents share a single source_country. The story's editorial perspective collapses to one party even when domain diversity reads high. Similar in shape to gate-2b (single editorial network) but indexed on source_country rather than network membership.
- [2026-05-06 story 183] Every module in story 183's render manifest carried asset_status='placeholder' because the v2-bridge (scripts/render-from-plan.js) does not fetch external assets — it sets placeholder unconditionally when module.asset.src is absent and never populates module.asset.src. This is a render-layer architectural gap, not a stage-3/4 plan defect, but it produces five render/missing-asset warns and caps subjective_quality at 3 regardless of story craft. — check: Add a stage-7 pre-flight check in video-learning/prompts/06-post-render-critic.md: if 100% of manifest.modules[].asset_status === 'placeholder', log a single bridge/architectural-gap info entry and downgrade per-module render/missing-asset findings to warn (not blocker) regardless of module kind, but cap subjective_quality at 3 — the visual layer cannot be evaluated. Until the bridge fetches assets, do not promote any story to 'published' from a 100%-placeholder manifest.
- [2026-05-06 story 183] The v2-bridge concentrated the entire 21.379s gap between the stage-4 plan total (53.0s) and the realised ElevenLabs TTS total (74.379s) into the close module (planned 7s, manifest 28.379s, drift +21.4s), leaving the outro on screen far longer than any single OutroLockup card can sensibly hold. — check: Add a stage-7 check: if any single module's (manifest_duration - 04_module-plan.duration_sec) > 5.0s while every other module's drift is <= 0.1s, log render/mistimed-cut and treat as iterate-blocker until the bridge re-balances. Bridge fix: scripts/render-from-plan.js should distribute the TTS-vs-plan delta proportionally across modules using TTS word-boundary timestamps, not absorb it into the final module.
- [2026-05-06 story 183] Synth surfaced ZERO structured numbers for story 183 (story.row.structured_numbers={}, primary_entities_enriched=[]) despite the source articles carrying the 19,247-vote margin, 85,000-vote margin, 79.6% turnout, and the 1977 streak-break year. Stage-2 evidence package recovered 8 numeric_facts directly from source_documents content. Same upstream-extractor weakness pattern that the seed 'extractor count-drop' entry warns about; this is a second observed firing for that class on a non-finance story. — check: Stage-2 gate already exists ('A drop >=50% is a blocker for stage 2' per known-failure-modes.md seed entry). It did not fire here because the gate triggers on a drop, not on a zero-from-zero synth output where stage-2 successfully recovers via direct content scan. Refine the seed gate in video-learning/prompts/02-evidence-package.md: when story.row.structured_numbers is empty AND 02_evidence stage-2 recovery surfaces >=3 numeric_facts, log a synth/extractor-empty warn (not a blocker — stage 2 succeeded), and route the row to 08_learning as upstream synth feedback automatically. This converts the manual surfacing in 05's infos[20] into a structured signal.
- [2026-05-06 story 187] Every module in the render reported asset_status='placeholder' because the v2-bridge has no external asset-fetch layer wired in, so a stage-4 plan with valid asset_hint values still produces an under-styled rendered piece. — check: Stage 7 prompt (video-learning/prompts/06-post-render-critic.md) should grow a 'bridge gap' clause: when manifest.bridge.version='v2-bridge' and asset_status='placeholder' on >=50% of modules, log a single render/bridge-asset-gap warn instead of one render/missing-asset warn per module, and explicitly suppress the 'blocker on hook/stakes missing-asset' rule so the story isn't iterated solely for the platform gap. Test: a regex over manifest.json for `"asset_status": "placeholder"` count >= modules.length/2 plus bridge.version='v2-bridge'.
- [2026-05-06 story 195] v2-bridge rendered every module of story 195 with asset_status='placeholder' despite stage 4 carrying numeric_fact_ref='defected_mps' and despite story.row containing wikipedia_thumbnail_url for Mann, Murmu, Chadha, and Punjab — the bridge does not perform any external asset fetch, so the visual layer is decoupled from the synth-side asset metadata that already exists. — check: Add a stage-7 check that, when manifest.bridge.version='v2-bridge' AND story.row.primary_entities_enriched[].wikipedia_thumbnail_url is populated for at least one named entity in any evidence module, the post-render critic must log a render/missing-asset warn citing the specific entity whose available thumbnail was not surfaced. Threshold: ≥1 named-entity beat with an available wiki_thumbnail rendered as placeholder.
- [2026-05-06 story 195] Stakes module (module_index=1) carried numeric_fact_ref='defected_mps' (value=6, the headline number of the video) but rendered as a placeholder NumberCard — the single most consequential visual asset in the video landed generic. — check: Add a stage-7 blocker (not warn) when component_type='NumberCard' AND asset_status='placeholder' AND a numeric_fact_ref is present. Rationale: a stakes/data module is the only kind for which the placeholder fallback strips load-bearing meaning from the frame. Lives in video-learning/prompts/06-post-render-critic.md § 'Asset / visual-safety check'.
- [2026-05-06 story 195] Outro module (module_index=5) rendered 20.4s for an 8.0s plan budget — a +12.4s drift, single-handedly pushing total runtime from planned 55.0s to actual 67.4s. The other five modules hit duration exactly. — check: Add a stage-6 (renderer) or stage-7 check: per-module |manifest_duration - plan_duration| > 2.0s logs a render/mistimed-cut warn naming the module index. Specifically for kind='close', drift > +5.0s suggests the renderer is silence-padding to flush voice or holding the end-card; route to a render/outro-pad failure subclass and ask whether the outro is data-determined or a renderer constant.
- [2026-05-06 story 197] Every module rendered with manifest.asset_status='placeholder' because the v2-bridge has no external asset fetch step; per the post-render critic's per-kind rule, this generates a blocker on the hook and stakes modules and warns on evidence/close, forcing decision='iterate' on every story rendered through v2-bridge regardless of script craft. — check: In tools/render via the v2-bridge, asset_status='placeholder' should be normalised to 'placeholder_by_design' until an asset-fetch step is added. The post-render critic prompt (video-learning/prompts/06-post-render-critic.md) should add a guard: if 100% of modules have asset_status='placeholder' AND manifest.bridge.version === 'v2-bridge', record once as infos[] 'render/architectural-gap' rather than fanning out as N blockers/warns. Failure class: render/missing-asset.
- [2026-05-06 story 199] v2-bridge rendered every module with asset_status='placeholder' on a story whose row.primary_entities_enriched carries wikipedia_thumbnail_url for at least 6 named entities (Chadha, Maliwal, Harbhajan Singh, AAP logo, BJP logo, Kejriwal) — second consecutive geopolitics story (after 195) where the synth-side asset metadata is decoupled from the v2 video output. The bridge has no external asset fetch step at all, regardless of asset_hint or numeric_fact_ref. — check: Promote story 195's stage-8 future_check to a playbook §6 decision rule (two-strike threshold met by story 199): 'When manifest.bridge.version='v2-bridge' AND ≥1 module has asset_status='placeholder' AND ≥1 named entity in 04_module-plan modules[].text has a populated row.primary_entities_enriched[].wikipedia_thumbnail_url, log render/missing-asset/bridge-gap as a warn (not blocker — architectural). When ≥2 stories in a row hit this, escalate the bridge-asset-fetch work item.' Threshold: 2 stories — already hit (195, 199).
- [2026-05-06 story 199] Second occurrence of stakes-NumberCard placeholder: module_index=1 (component_type='NumberCard', numeric_fact_ref='mps_remaining' value=3) rendered as placeholder on the load-bearing '10 → 3' beat. Story 195 was the first occurrence (numeric_fact_ref='defected_mps', value=6); story 195's stage-8 entries[1] proposed promoting this exact pattern from warn to stage-7 blocker. Story 199 logged it as a blocker per that proposal — the future_check is empirically validated. — check: Promote story 195's entries[1] proposal from prompt_proposal to a playbook §2 failure-class rule: add a row to the failure taxonomy table — class 'render/missing-asset/numbercard-placeholder', manifests as 'component_type=NumberCard with numeric_fact_ref AND asset_status=placeholder', check 'Stage 7 logs as blocker; bridge must populate the numeric value as a NumberCard data graphic before clearing.' Two-strike rule satisfied (195 + 199); ready to graduate from prompt to playbook.
- [2026-05-06 story 199] Outro-pad drift hit +21.888s on a 10s-budget close — third consecutive incident (195: +12.4s, 197: +15.4s, 199: +21.9s) and now growing. The bridge pins the entire voice-track tail to the close module rather than re-flowing across modules. On story 199 the close occupies 31.888s / 83.388s = 38% of the runtime. — check: Stage 4 must lower the WPM assumption for duration calc from 165 to 120 wpm. Concretely in video-learning/prompts/04-module-plan.md § Duration: change `target_total = word_count / 165 * 60` to `target_total = word_count / 120 * 60`. Story 199 effective WPM = 118.6; story 197 = 132.8; story 195 ran similar. The 165 figure (playbook §1.1 Pace) is a *spoken-word target* for script length, not the *measured ElevenLabs cadence*. Stage 4 plan-budget should use the measured cadence so the close module's plan budget tracks the actual TTS dwell. Two-strike rule satisfied (197 noted the gap as an info; 199 confirms with a 28% drift).
- [2026-05-06 story 206] v2-bridge produced asset_status='placeholder' for all six modules, including the hook and stakes — bridge has no asset-fetch / asset-resolution code path, so every render is decoupled from the plan's asset_hint regardless of how strong the upstream evidence is. — check: Stage 7 must keep flagging asset_status='placeholder' on hook/stakes as render/missing-asset blockers (already does, per video-learning/prompts/06-post-render-critic.md 'Per-module check'). Until v2-bridge implements asset selection, every story routed through it will iterate at stage 7 — operator should treat this as a system-level signal to prioritise the bridge asset path rather than as a per-story script defect. Add a check at render-time entry: if manifest.bridge.version='v2-bridge' and any module.asset_class != 'mute', emit a startup warning that the bridge will placeholder all assets.
- [2026-05-06 story 206] OutroLockup module absorbed +17.754s of trailing silent hold (planned 10s, manifest 27.754s) because the bridge sizes the outro to fill total runtime rather than ending after the close VO finishes. Net effect: ~16s of frozen outro after the narration ends at 72.354s. — check: Add a stage-7 module-finding rule (already partially implemented): if module.role='outro' AND |duration_drift_sec| > 5.0, log render/mistimed-cut as a blocker (currently warn). Add a render-time invariant in the bridge: outro_duration = max(close_vo_duration_sec + 1.0, 4.0) — never pad to fill an external total runtime target. Record check in video-learning/prompts/06-post-render-critic.md '§Pacing check'.
- [2026-05-06 story 206] Module-boundary plan durations diverged from TTS reality after module 1: planned 11.5s/12s/10s/10s for evidence/evidence/quote/close, but ElevenLabs cadence pushed actual narrated boundaries 5–10s past plan boundaries (evidence-2 narration runs 28.7–45.5s vs plan 23–35s; quote runs 53.1–60.4s vs plan 45–55s). — check: Add a stage-4 sanity check in video-learning/prompts/04-module-plan.md: estimate per-module narration duration from word count at 2.7 words/sec (the empirical ElevenLabs cadence on this row: 151 words / 56.0s spoken-only = 2.7 wps), not the historical 2.5 wps. If sum(modules[].duration_sec) is within 2s of estimated TTS total, accept; otherwise re-budget. Threshold: |plan_total - 0.37 * word_count| <= 2.0 seconds.
- [2026-05-06 story 213] v2-bridge rendered every module of story 213 with asset_status='placeholder' (7/7 modules), turning the post-render check's hook/stakes asset rules into automatic blockers regardless of plan quality. — check: Add a manifest sanity check at the top of stage 7 in video-learning/prompts/06-post-render-critic.md: if manifest.bridge.version === 'v2-bridge' AND every module has asset_status='placeholder', surface this as a single 'render/v2-bridge-asset-gap' info finding plus per-kind warns/blockers per the existing rule. Operator should treat the architectural gap as a separate decision axis from per-story rejection — not scrap solely for this. Concretely: extend the prompt's 'Asset / visual-safety check' bullet for placeholders to read 'unless every module is placeholder AND manifest.bridge has no asset-fetch path declared, in which case downgrade hook/stakes blockers to warns and log one render/v2-bridge-asset-gap blocker against the bridge, not the story plan.'
- [2026-05-06 story 213] Close/OutroLockup module overran by +25.4s (planned 8.7s, manifest 34.139s) because the bridge absorbed the entire voice-vs-plan timing residual into the outro slot rather than redistributing across body modules — total runtime 90.5s vs plan 65.1s. — check: Add a stage-4 check that compares the planned total module duration to an estimate of the actual TTS read time using a per-voice WPM constant (elevenlabs reads slower than the 165wpm playbook baseline). If estimated_voice_duration - planned_total > 5s, surface as a stage-5 warn so the script can be tightened before render. Concretely: in video-learning/prompts/04-module-plan.md add 'compute estimated_voice_duration = word_count / VOICE_WPM where VOICE_WPM defaults to 145 for elevenlabs' and require the plan's total_duration_sec to be within ±3s of that estimate, not the 165wpm script target. Threshold: |estimated_voice_duration - sum(modules.duration_sec)| <= 3.0.
- [2026-05-06 story 216] Every module rendered with asset_status='placeholder' because the v2-bridge has no asset-fetch / NumberCard-binding step, so 'data' modules with valid numeric_fact_refs (M0 tvk_seats_won, M1 tvk_short_of_majority, M3 dmk_seats_won) ship without on-frame numerics and 'mute' modules report graphic-placeholder instead of an honored mute frame. — check: Stage 7 must scan manifest.json for asset_status counts; if 100% of modules are 'placeholder' (architectural rather than per-module sourcing failure), classify as render/missing-asset-architectural and escalate the bridge-asset-fetch gap to the operator's stage-7 report. Add a one-line manifest summary 'asset_ok / asset_fallback / asset_placeholder counts' to manifest.json so the post-render critic can compute the architectural-vs-per-module distinction in O(1).
- [2026-05-06 story 216] Outro module (M5, kind=close) ran 24.939s vs planned 8.0s — a +16.9s drift on a single module — driving total render duration to 72.939s and making the weakest beat (un-date-anchored close) the longest beat in the video. — check: Stage 7: any module whose duration_drift_sec exceeds ±2.0 (currently the threshold is ±1.0 for pacing observation; tighten to ±2.0 for blocker on a single module) is a render/overflow blocker. Additionally: in stage 4, when total_duration_sec is computed, the close module's duration should be capped at min(planned_duration, total_voice_remaining_after_other_modules + 1.0s) to prevent the OutroLockup from absorbing all leftover composition time.
- [2026-05-06 story 222] Voice total 81.037s vs planned 60.4s — a +20.637s overrun on a 167-word script, the entire overrun pinned to the outro (module 5 plan=8.4s, manifest=29.437s, drift +21.037s). Effective ElevenLabs WPM = 123.6, the second consecutive geopolitics story to run well under the 165-wpm assumption used in stage 4's duration calculation (story 197 ran at 132.8 wpm). — check: Two-strike threshold reached on the wpm-drift class. Promote story 197's prompt_proposal: edit video-learning/prompts/04-module-plan.md duration formula from 'word_count / 165 * 60' to 'word_count / 140 * 60' (or expose as a constant `WPM_ASSUMPTION = 140` and reference it). Add a stage-5 check C13a: assert manifest-vs-plan voice overrun observed in last N geopolitics renders (rolling), and warn if predicted overrun > 10s. Failure class: render/wpm-drift (sub-class of module/duration-drift). Separately, the bridge's voice-tail-pin behavior should be addressed: re-flow the overrun across the last 2-3 modules rather than concentrating it on the outro alone.
- [2026-05-06 story 222] All 6 modules rendered with manifest.asset_status='placeholder' because the v2-bridge has no external asset fetch step. Per the post-render critic's per-kind rule, this would normally generate blockers on the hook and stakes modules and warns on evidence/close; the special-case 'render/architectural-gap' guard recommended in story 197's 08_learning entry 2 was applied — placeholders recorded once as info rather than fanning out to N blockers — but the underlying gap remains: every v2-bridge render ships visually below the bar regardless of script craft. — check: The post-197 prompt-side guard now works correctly (no spurious blocker fan-out). The remaining work is system-level: add an asset-fetch step to azure-functions/v2-bridge OR wire stage 4 asset_hints into a Remotion-side asset selector that pulls from a curated graphic library (labelled maps, generic data-viz templates, institutional seals). Until shipped, every v2-bridge story's subjective_quality is capped at ~3 because the visual evidence layer can't honor any asset_hint. Failure class: render/missing-asset (architectural).
- [2026-05-06 story 222] Story 222's source set is 6/6 India-based outlets (timesofindia.indiatimes.com x4, indiatoday.in x2) for a U.S.-Iran-Pakistan geopolitics story with no U.S., Iranian, or Pakistani primary state-readout sourcing. Stage 2 cleared the row (gate-3 source diversity passed because 2 distinct domain roots ≥ minimum) but stage 5 surfaced this as info context/source-geography. Survived to render unchanged, and although it did not produce a visible regression, it is a real source-quality weakness — the entire script's authority chain runs through wire-rewrites of U.S./Iranian official statements as filtered by Indian outlets. — check: Add a stage-2 publishability sub-gate in video-learning/prompts/02-evidence-package.md: for stories whose primary_geos do NOT include the source_documents[].source_country majority country, require ≥1 source from one of the named state-actor countries OR an authoritative international wire (Reuters, AFP, AP) for status='ok'; otherwise downgrade to status='ok-with-caveat' and require stage 4 to plan an explicit 'sourcing caveat' on-frame in the close, OR set status='insufficient' if the geopolitics row has zero state-actor sourcing AND zero international wire sourcing. Failure class: sourcing/geographic-mismatch (new sub-class of sourcing/single).
- [2026-05-09 story 181] The synth (azure-functions/) emits `story.row.primary_entities = ['dinesh trivedi', 'pranay verma', 'tarique rahman', 'bangladesh', 'west bengal', ...]` (10 entries) but `primary_entities_enriched = []` (empty). With no enriched entries, the v2-bridge's asset-fetch layer cannot resolve a Wikipedia pageimage for any named person, place, or institution — every module ships without a real visual. Operator review of the resulting MP4 was rejected as "absolute nonsense" / "mostly blank". The asset-fetch layer in the renderer is one half of the fix; the other half is upstream — `azure-functions/article-clusterer` or `story-synthesizer` must populate `primary_entities_enriched[]` with at least `name`, `type`, `wikipedia_url`, and `image_url` per entity, so the renderer has a deterministic source for visuals. The currently empty array is the canonical reason every v2 render reads as visually nonsense regardless of script craft. — check: Add a synth-side validation in `azure-functions/lib/` that asserts `primary_entities_enriched.length >= max(1, primary_entities.length / 2)` before a story row is marked `quiz_candidate=true`. Add a stage-2 publishability gate (gate-9?) in `video-learning/prompts/02-evidence-package.md`: status='insufficient' if `story.row.primary_entities_enriched.length === 0` AND `story.row.story_type !== 'finance_markets'` (finance can lean on number-card visuals). Add a render-time fallback ladder in `scripts/render-from-plan.js`: for each module, try (1) primary_entities_enriched[].image_url, (2) Wikipedia pageimage REST API call against any wikipedia_url present, (3) curated concept stock library by asset_hint + module text keywords, (4) country flag image — fail loud (do not render with placeholder) if all four miss. Failure class: synth/empty-entities-enriched (upstream); render/missing-asset-fallback (downstream). This is the upstream root cause of the post-181 production-quality rejection.
