# Dependencies
node_modules/

# Python — tools/ uses Python; ignore caches and per-story workspaces.
__pycache__/
*.pyc
*.pyo
stories/

# Render outputs — videos, frozen fixtures, render-props, narration audio.
# Never commit, can be regenerated from source.
output/

# Per-job static-file copies the renderer hands to Remotion.
# Regenerated on every run.
public/jobs/

# Local env (already gitignored at repo root, but defensive).
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
*.swp
*.swo
Thumbs.db
